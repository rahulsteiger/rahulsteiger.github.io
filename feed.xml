<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://rahulsteiger.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://rahulsteiger.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-12-26T11:42:10+00:00</updated><id>https://rahulsteiger.github.io/feed.xml</id><title type="html">blank</title><subtitle>A collection of my projects, publications, and blog posts. </subtitle><entry><title type="html">CUDA Programming (Fundamentals)</title><link href="https://rahulsteiger.github.io/blog/2025/cuda-programming/" rel="alternate" type="text/html" title="CUDA Programming (Fundamentals)"/><published>2025-10-12T00:00:00+00:00</published><updated>2025-10-12T00:00:00+00:00</updated><id>https://rahulsteiger.github.io/blog/2025/cuda-programming</id><content type="html" xml:base="https://rahulsteiger.github.io/blog/2025/cuda-programming/"><![CDATA[<p>These are my notes for the course CUDA Programming Course – High-Performance Computing with GPUs on <a href="https://www.youtube.com/watch?v=86FAWCzIe_4&amp;t">YouTube</a> Chapters 1-6. The authors made the course code available on <a href="https://github.com/Infatoshi/cuda-course">GitHub</a>. I will be working on the ALPS cluster with a GH200 System.</p> <h2 id="setup">Setup</h2> <p>The first four chapters are mostly an overview of GPU Programming, the Deep Learning ecosystem and a recap of C++.</p> <p>I am running my experiments in a standard container from the NVIDIA registry, specifically I am using the following one: nvcr.io/nvidia/cuda:12.9.0-devel-ubuntu24.04. This means that I will work with CUDA 12.9.0 for this course. As a first step, the course recommends taking a look around the NVIDIA CUDA samples on <a href="https://github.com/NVIDIA/cuda-samples">GitHub</a>. One thing to keep in mind is that you select the correct release, i.e. CUDA Samples v12.9.</p> <p>Looking at the utilities, I can use the sample in <code class="language-plaintext highlighter-rouge">Samples/1_Utilities/deviceQuery</code> to print the properties of the system. The system has 4 GH200 GPUs, however to keep things short, here is the overview of a single GH200 GPU.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Device 0: <span class="s2">"NVIDIA GH200 120GB"</span>
  CUDA Driver Version / Runtime Version          12.9 / 12.9
  CUDA Capability Major/Minor version number:    9.0
  Total amount of global memory:                 96768 MBytes <span class="o">(</span>101468602368 bytes<span class="o">)</span>
  <span class="o">(</span>132<span class="o">)</span> Multiprocessors, <span class="o">(</span>128<span class="o">)</span> CUDA Cores/MP:    16896 CUDA Cores
  GPU Max Clock rate:                            1980 MHz <span class="o">(</span>1.98 GHz<span class="o">)</span>
  Memory Clock rate:                             2619 Mhz
  Memory Bus Width:                              6144-bit
  L2 Cache Size:                                 62914560 bytes
  Maximum Texture Dimension Size <span class="o">(</span>x,y,z<span class="o">)</span>         <span class="nv">1D</span><span class="o">=(</span>131072<span class="o">)</span>, <span class="nv">2D</span><span class="o">=(</span>131072, 65536<span class="o">)</span>, <span class="nv">3D</span><span class="o">=(</span>16384, 16384, 16384<span class="o">)</span>
  Maximum Layered 1D Texture Size, <span class="o">(</span>num<span class="o">)</span> layers  <span class="nv">1D</span><span class="o">=(</span>32768<span class="o">)</span>, 2048 layers
  Maximum Layered 2D Texture Size, <span class="o">(</span>num<span class="o">)</span> layers  <span class="nv">2D</span><span class="o">=(</span>32768, 32768<span class="o">)</span>, 2048 layers
  Total amount of constant memory:               65536 bytes
  Total amount of shared memory per block:       49152 bytes
  Total shared memory per multiprocessor:        233472 bytes
  Total number of registers available per block: 65536
  Warp size:                                     32
  Maximum number of threads per multiprocessor:  2048
  Maximum number of threads per block:           1024
  Max dimension size of a thread block <span class="o">(</span>x,y,z<span class="o">)</span>: <span class="o">(</span>1024, 1024, 64<span class="o">)</span>
  Max dimension size of a grid size    <span class="o">(</span>x,y,z<span class="o">)</span>: <span class="o">(</span>2147483647, 65535, 65535<span class="o">)</span>
  Maximum memory pitch:                          2147483647 bytes
  Texture alignment:                             512 bytes
  Concurrent copy and kernel execution:          Yes with 3 copy engine<span class="o">(</span>s<span class="o">)</span>
  Run <span class="nb">time </span>limit on kernels:                     No
  Integrated GPU sharing Host Memory:            No
  Support host page-locked memory mapping:       Yes
  Alignment requirement <span class="k">for </span>Surfaces:            Yes
  Device has ECC support:                        Enabled
  Device supports Unified Addressing <span class="o">(</span>UVA<span class="o">)</span>:      Yes
  Device supports Managed Memory:                Yes
  Device supports Compute Preemption:            Yes
  Supports Cooperative Kernel Launch:            Yes
  Supports MultiDevice Co-op Kernel Launch:      Yes
  Device PCI Domain ID / Bus ID / location ID:   9 / 1 / 0
</code></pre></div></div> <p>The GH200 system has a higher compute capability compared to the one used in the Tutorial. This means that the system I am working on has some additional features that are not covered in the tutorial. How to properly use those features is for me to figure out and will be the topic of a future blog post.</p> <p>Additionally, I created a private fork of the course’s repo following the instructions <a href="https://gist.github.com/0xjac/85097472043b697ab57ba1b1c7530274">here</a>.</p> <h2 id="the-cuda-basics">The CUDA basics</h2> <p>The fifth chapter covers CUDA fundamentals.</p> <h3 id="thread-hierarchy">Thread Hierarchy</h3> <p><strong>Thread:</strong></p> <ul> <li>Smallest unit of execution in CUDA</li> <li>Executes kernel code independently</li> <li>Has local memory (registers) private to the thread</li> </ul> <p><strong>Block:</strong></p> <ul> <li>Group of threads that can cooperate and synchronize</li> <li>Threads share data via shared memory</li> <li>Can be 1D, 2D, or 3D</li> <li>Enables efficient memory access through coordination of threads</li> </ul> <p><strong>Grid:</strong></p> <ul> <li>Complete set of threads launched for a kernel invocation</li> <li>Grid dimensions determine the number of blocks created</li> <li>Each block represents a subset of the problem that can be solved independently</li> <li>Can be 1D, 2D, or 3D</li> </ul> <p><strong>CUDA Variables:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">gridDim</code>: number of blocks in the grid</li> <li><code class="language-plaintext highlighter-rouge">blockIdx</code>: index of the current block</li> <li><code class="language-plaintext highlighter-rouge">blockDim</code>: number of threads per block</li> <li><code class="language-plaintext highlighter-rouge">threadIdx</code>: index of the current thread within its block</li> </ul> <p>There are a <code class="language-plaintext highlighter-rouge">dim3</code> (3D), <code class="language-plaintext highlighter-rouge">dim2</code> (2D), int (1D) types that you can pass to the kernel launch configuration.</p> <p><strong>Warps:</strong></p> <ul> <li>Each block is divided into warps</li> <li>A warp executes 32 threads in parallel (SIMT)</li> <li>All threads in a warp execute the same instruction simultaneously</li> </ul> <p>Note: CUDA Compute Capability 9.0 introduced the notion of Clusters. In short, Clusters are a group of thread blocks that can communicate with each other. Since this course only considers GPUs with lower Compute Capabilities, this will be the topic of another future blog post.</p> <h3 id="indexing">Indexing</h3> <p>This is a cool example and analogy that explains quite clearly how CUDA threads are organized.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">whoami</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">block_id</span> <span class="o">=</span>
        <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span>    <span class="c1">// apartment number on this floor (points across)</span>
        <span class="n">blockIdx</span><span class="p">.</span><span class="n">y</span> <span class="o">*</span> <span class="n">gridDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span>    <span class="c1">// floor number in this building (rows high)</span>
        <span class="n">blockIdx</span><span class="p">.</span><span class="n">z</span> <span class="o">*</span> <span class="n">gridDim</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">gridDim</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>   <span class="c1">// building number in this city (panes deep)</span>

    <span class="kt">int</span> <span class="n">block_offset</span> <span class="o">=</span>
        <span class="n">block_id</span> <span class="o">*</span> <span class="c1">// times our apartment number</span>
        <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">y</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">z</span><span class="p">;</span> <span class="c1">// total threads per block (people per apartment)</span>

    <span class="kt">int</span> <span class="n">thread_offset</span> <span class="o">=</span>
        <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span>
        <span class="n">threadIdx</span><span class="p">.</span><span class="n">y</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span>
        <span class="n">threadIdx</span><span class="p">.</span><span class="n">z</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>

    <span class="kt">int</span> <span class="n">id</span> <span class="o">=</span> <span class="n">block_offset</span> <span class="o">+</span> <span class="n">thread_offset</span><span class="p">;</span> <span class="c1">// global person id in the entire apartment complex</span>

    <span class="n">printf</span><span class="p">(</span><span class="s">"%04d | Block(%d %d %d) = %3d | Thread(%d %d %d) = %3d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
        <span class="n">id</span><span class="p">,</span>
        <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">y</span><span class="p">,</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">z</span><span class="p">,</span> <span class="n">block_id</span><span class="p">,</span>
        <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">y</span><span class="p">,</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">z</span><span class="p">,</span> <span class="n">thread_offset</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="memory-hierarchy">Memory Hierarchy</h3> <p><strong>Global Memory</strong>:</p> <ul> <li>Main GPU memory, accessible by all threads.</li> <li><strong>Largest</strong> but <strong>slowest</strong>.</li> <li>For data shared across threads (e.g., arrays).</li> </ul> <p><strong>Shared Memory</strong>:</p> <ul> <li>Fast, small memory shared within a block.</li> <li>For frequent data exchange between threads in a block (e.g., temp variables).</li> </ul> <p><strong>Registers</strong>:</p> <ul> <li>Fastest, private to each thread.</li> <li>For local variables (e.g., loop counters).</li> <li>Limited, use carefully.</li> </ul> <p><strong>Constant Memory</strong>:</p> <ul> <li>Read-only, cached, accessible by all threads.</li> <li>For constants/configuration that don’t change during execution.</li> </ul> <p><strong>Local Memory</strong>:</p> <ul> <li>Used when registers overflow.</li> <li><strong>Slow</strong> (stored in global memory).</li> <li>Avoid if possible (e.g., large arrays).</li> </ul> <h3 id="cudamalloc--cudamemcpy--cudamallochost">cudaMalloc &amp; cudaMemcpy &amp; cudaMallocHost</h3> <p>A good convention for data variable names is to start them with <code class="language-plaintext highlighter-rouge">h_</code> if they are on the Host (CPU) and start them with <code class="language-plaintext highlighter-rouge">d_</code> if they are on the Device (GPU). Allocating memory on the host and device can be done as follows:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">size</span> <span class="o">=</span> <span class="mi">2048</span><span class="p">;</span>
<span class="kt">float</span> <span class="o">*</span><span class="n">d_x</span><span class="p">;</span>
<span class="n">cudaMalloc</span><span class="p">(</span><span class="o">&amp;</span><span class="n">d_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">));</span>
</code></pre></div></div> <p>To transfer data between host and device, use <code class="language-plaintext highlighter-rouge">cudaMemcpy</code>:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">size</span> <span class="o">=</span> <span class="mi">2048</span><span class="p">;</span>
<span class="kt">float</span> <span class="o">*</span><span class="n">d_x</span><span class="p">,</span> <span class="o">*</span><span class="n">h_x</span><span class="p">;</span>
<span class="n">cudaMalloc</span><span class="p">(</span><span class="o">&amp;</span><span class="n">d_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">));</span>
<span class="n">h_x</span> <span class="o">=</span> <span class="p">(</span><span class="kt">float</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">));</span>

<span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">d_x</span><span class="p">,</span> <span class="n">h_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToDevice</span><span class="p">);</span>   <span class="c1">// Host → Device</span>
<span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">h_x</span><span class="p">,</span> <span class="n">d_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyDeviceToHost</span><span class="p">);</span>   <span class="c1">// Device → Host</span>
<span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">h_x</span><span class="p">,</span> <span class="n">h_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToHost</span><span class="p">);</span>     <span class="c1">// Host → Host, equivalent to memcpy</span>
<span class="n">cudaMemcpy</span><span class="p">(</span><span class="n">d_x</span><span class="p">,</span> <span class="n">d_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyDeviceToDevice</span><span class="p">);</span> <span class="c1">// Device → Device</span>
</code></pre></div></div> <p>Pinned (page-locked) memory is host memory that the OS cannot move, allowing the GPU to access it directly and transfer data faster. Allocate pinned memory with:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">float</span><span class="o">*</span> <span class="n">h_data</span><span class="p">;</span>
<span class="n">cudaMallocHost</span><span class="p">((</span><span class="kt">void</span><span class="o">**</span><span class="p">)</span><span class="o">&amp;</span><span class="n">h_data</span><span class="p">,</span> <span class="n">size</span><span class="p">);</span>
</code></pre></div></div> <h3 id="launching-a-kernel">Launching a kernel:</h3> <p>One can launch a kernel with</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;&lt;&lt;</span><span class="n">gridDim</span><span class="p">,</span> <span class="n">blockDim</span><span class="p">,</span> <span class="n">Ns</span><span class="p">,</span> <span class="n">S</span><span class="o">&gt;&gt;&gt;</span><span class="n">my_kernel</span><span class="p">(</span><span class="n">my_param</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">gridDim</code> defines the size of the block grid</li> <li><code class="language-plaintext highlighter-rouge">blockDim</code> defines the size of each block</li> <li><code class="language-plaintext highlighter-rouge">Ns</code> specifies the number of bytes in shared memory that are dynamically allocated per block (usually omitted)</li> <li><code class="language-plaintext highlighter-rouge">S</code> specifies the associated CUDA Stream (see definition later)</li> </ul> <h3 id="thread-synchronization">Thread Synchronization</h3> <p>By default, the CPU and GPU execute independently. To ensure the CPU waits for all GPU work to finish (e.g., after launching a kernel), call <code class="language-plaintext highlighter-rouge">cudaDeviceSynchronize()</code>. This blocks the CPU until all preceding GPU tasks are complete.</p> <p>In many cases, this is overkill since you might want to do some more fine-grained synchronization. This can be done as follows:</p> <ul> <li>Synchronize all threads in a block: <code class="language-plaintext highlighter-rouge">__syncthreads()</code>.</li> <li>Synchronize all threads in a warp: <code class="language-plaintext highlighter-rouge">__syncwarps()</code>.</li> </ul> <h3 id="vector-addition">Vector addition</h3> <p>Standard C:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">vector_add_cpu</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">b</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">c</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span>
    <span class="n">c</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">+</span> <span class="n">b</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
<span class="p">}</span>
</code></pre></div></div> <p>CUDA kernel:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">vector_add_gpu</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">b</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">c</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">)</span>
    <span class="n">c</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">+</span> <span class="n">b</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
<span class="p">}</span>
</code></pre></div></div> <p>Assuming <code class="language-plaintext highlighter-rouge">d_a</code>, <code class="language-plaintext highlighter-rouge">d_b</code>, and <code class="language-plaintext highlighter-rouge">d_c</code> are device arrays of length <code class="language-plaintext highlighter-rouge">N</code>, launch the kernel:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">num_blocks</span> <span class="o">=</span> <span class="p">(</span><span class="n">N</span> <span class="o">+</span> <span class="n">BLOCK_SIZE</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">BLOCK_SIZE</span><span class="p">;</span>
<span class="n">vector_add_gpu</span><span class="o">&lt;&lt;&lt;</span><span class="n">num_blocks</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d_a</span><span class="p">,</span> <span class="n">d_b</span><span class="p">,</span> <span class="n">d_c</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
</code></pre></div></div> <p>This splits the work into blocks, each handling up to <code class="language-plaintext highlighter-rouge">BLOCK_SIZE</code> elements.</p> <p>Note: Use the simplest indexing for best performance. For example, 3D indexing:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">vector_add_gpu_3d</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">a</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">b</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">c</span><span class="p">,</span> <span class="kt">int</span> <span class="n">nx</span><span class="p">,</span> <span class="kt">int</span> <span class="n">ny</span><span class="p">,</span> <span class="kt">int</span> <span class="n">nz</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">j</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">y</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">y</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">k</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">z</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">z</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">z</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">nx</span> <span class="o">&amp;&amp;</span> <span class="n">j</span> <span class="o">&lt;</span> <span class="n">ny</span> <span class="o">&amp;&amp;</span> <span class="n">k</span> <span class="o">&lt;</span> <span class="n">nz</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">idx</span> <span class="o">=</span> <span class="n">i</span> <span class="o">+</span> <span class="n">j</span> <span class="o">*</span> <span class="n">nx</span> <span class="o">+</span> <span class="n">k</span> <span class="o">*</span> <span class="n">nx</span> <span class="o">*</span> <span class="n">ny</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">&lt;</span> <span class="n">nx</span> <span class="o">*</span> <span class="n">ny</span> <span class="o">*</span> <span class="n">nz</span><span class="p">)</span>
      <span class="n">c</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">+</span> <span class="n">b</span><span class="p">[</span><span class="n">idx</span><span class="p">];</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>3D indexing adds overhead and reduces performance. For a vector with 1,000,000 elements:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Speedup <span class="o">(</span>CPU vs GPU 1D<span class="o">)</span>: 365.5x
Speedup <span class="o">(</span>CPU vs GPU 3D<span class="o">)</span>: 308.6x
</code></pre></div></div> <p>That’s over 15% slower just from using 3D indexing!</p> <h3 id="nsight-systems--compute">Nsight systems &amp; compute</h3> <p>NVIDIA provides powerful tools for profiling and analyzing CUDA applications. Use <a href="https://developer.nvidia.com/nsight-systems">Nsight Systems</a> to visualize application performance and <a href="https://developer.nvidia.com/nsight-compute">Nsight Compute</a> for detailed kernel analysis. For command-line profiling, try <a href="https://docs.nvidia.com/nsight-systems/UserGuide/index.html#cli-launch">NVIDIA nsys</a>. These tools help you identify bottlenecks and optimize your GPU code efficiently.</p> <h3 id="atomic-operations">Atomic operations</h3> <p>Atomic operations in CUDA ensure that updates to shared memory are performed safely, preventing race conditions when multiple threads modify the same variable. While they can reduce parallel efficiency, they are essential for correctness in concurrent code.</p> <p>CUDA provides built-in atomic functions like <code class="language-plaintext highlighter-rouge">atomicAdd</code>, <code class="language-plaintext highlighter-rouge">atomicSub</code>, <code class="language-plaintext highlighter-rouge">atomicMin</code>, and <code class="language-plaintext highlighter-rouge">atomicMax</code> for integers and floats in global or shared memory.</p> <p>To count the number of positive elements in an array, each thread checks one element and atomically increments a shared counter:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">countPositiveAtomic</span><span class="p">(</span><span class="k">const</span> <span class="kt">int</span><span class="o">*</span> <span class="n">arr</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">counter</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span> <span class="o">&amp;&amp;</span> <span class="n">arr</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">atomicAdd</span><span class="p">(</span><span class="n">counter</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>This guarantees that every increment to <code class="language-plaintext highlighter-rouge">counter</code> is correct, even with thousands of threads running in parallel.</p> <h3 id="cuda-streams">CUDA Streams</h3> <p>CUDA Streams allow you to run multiple sequences of operations on the GPU. Operations within a single stream are executed in order, but different streams can run concurrently. This enables overlapping of computation and memory transfers, improving GPU utilization and overall performance.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cudaStream_t</span> <span class="n">stream1</span><span class="p">,</span> <span class="n">stream2</span><span class="p">;</span>
<span class="n">cudaStreamCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">stream1</span><span class="p">);</span>
<span class="n">cudaStreamCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">stream2</span><span class="p">);</span>

<span class="c1">// Launch a kernel in stream1</span>
<span class="n">vector_add_gpu</span><span class="o">&lt;&lt;&lt;</span><span class="n">num_blocks</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">stream1</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d_a</span><span class="p">,</span> <span class="n">d_b</span><span class="p">,</span> <span class="n">d_c</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>

<span class="c1">// Perform a memory copy in stream2</span>
<span class="n">cudaMemcpyAsync</span><span class="p">(</span><span class="n">d_x</span><span class="p">,</span> <span class="n">h_x</span><span class="p">,</span> <span class="n">size</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToDevice</span><span class="p">,</span> <span class="n">stream2</span><span class="p">);</span>
</code></pre></div></div> <p>CUDA Events are used to mark specific points in the execution timeline. They enable synchronization between streams, so you can ensure that an operation in one stream starts only after an event (such as completion of a previous operation) in another stream has occurred. The following example shows how to overlap computation and data loading:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cudaEvent_t</span> <span class="n">event</span><span class="p">;</span>
<span class="n">cudaEventCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">event</span><span class="p">);</span>

<span class="c1">// Launch a kernel in stream1</span>
<span class="n">vector_add_gpu</span><span class="o">&lt;&lt;&lt;</span><span class="n">num_blocks</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">stream1</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d_a</span><span class="p">,</span> <span class="n">d_b</span><span class="p">,</span> <span class="n">d_c</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>

<span class="c1">// Perform a memory copy in stream2</span>
<span class="n">cudaMemcpyAsync</span><span class="p">(</span><span class="n">d_x</span><span class="p">,</span> <span class="n">h_x</span><span class="p">,</span> <span class="n">N</span> <span class="o">*</span> <span class="nf">sizeof</span><span class="p">(</span><span class="kt">float</span><span class="p">),</span> <span class="n">cudaMemcpyHostToDevice</span><span class="p">,</span> <span class="n">stream2</span><span class="p">);</span>

<span class="c1">// Record event on stream2 after the copy</span>
<span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">event</span><span class="p">,</span> <span class="n">stream2</span><span class="p">);</span>

<span class="c1">// Make stream1 wait for the event (i.e., wait for the copy to finish)</span>
<span class="n">cudaStreamWaitEvent</span><span class="p">(</span><span class="n">stream1</span><span class="p">,</span> <span class="n">event</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>

<span class="c1">// Launch a kernel with new data in stream1</span>
<span class="n">vector_add_gpu</span><span class="o">&lt;&lt;&lt;</span><span class="n">num_blocks</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">stream1</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">d_x</span><span class="p">,</span> <span class="n">d_c</span><span class="p">,</span> <span class="n">d_c</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
</code></pre></div></div> <p>You can also use Events to measure the time it takes to execute a kernel:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cudaEvent_t</span> <span class="n">start</span><span class="p">,</span> <span class="n">stop</span><span class="p">;</span>
<span class="n">cudaEventCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">start</span><span class="p">);</span>
<span class="n">cudaEventCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">stop</span><span class="p">);</span>

<span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">start</span><span class="p">,</span> <span class="n">stream</span><span class="p">);</span>
<span class="n">kernel</span><span class="o">&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">stream</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">args</span><span class="p">);</span>
<span class="n">cudaEventRecord</span><span class="p">(</span><span class="n">stop</span><span class="p">,</span> <span class="n">stream</span><span class="p">);</span>

<span class="n">cudaEventSynchronize</span><span class="p">(</span><span class="n">stop</span><span class="p">);</span>
<span class="kt">float</span> <span class="n">milliseconds</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="n">cudaEventElapsedTime</span><span class="p">(</span><span class="o">&amp;</span><span class="n">milliseconds</span><span class="p">,</span> <span class="n">start</span><span class="p">,</span> <span class="n">stop</span><span class="p">);</span>
</code></pre></div></div> <p>You can synchronize a specific stream with <code class="language-plaintext highlighter-rouge">cudaStreamSynchronize(stream1)</code>, which blocks the host until all operations in that stream are complete. To synchronize all streams and the entire device, use <code class="language-plaintext highlighter-rouge">cudaDeviceSynchronize()</code>.</p> <h3 id="callbacks">Callbacks</h3> <p>Callbacks let you create a pipeline where the completion of a GPU operation automatically triggers a CPU task. This enables efficient coordination between GPU and CPU workloads.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="n">CUDART_CB</span> <span class="nf">MyCallback</span><span class="p">(</span><span class="n">cudaStream_t</span> <span class="n">stream</span><span class="p">,</span> <span class="n">cudaError_t</span> <span class="n">status</span><span class="p">,</span> <span class="kt">void</span> <span class="o">*</span><span class="n">userData</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"GPU operation completed</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="c1">// Trigger next batch of work</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="cuda-api">CUDA API</h2> <p>NVIDIA provides libraries with highly optimized CUDA kernels for GPU computing.</p> <h3 id="cublas">cuBLAS</h3> <p>cuBLAS is NVIDIA’s industry-standard library for BLAS and GEMM routines, highly optimized for GPU performance. It supports operation fusion for greater efficiency.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__half</span> <span class="n">alpha_h</span> <span class="o">=</span> <span class="n">__float2half</span><span class="p">(</span><span class="mf">1.0</span><span class="n">f</span><span class="p">),</span> <span class="n">beta_h</span> <span class="o">=</span> <span class="n">__float2half</span><span class="p">(</span><span class="mf">0.0</span><span class="n">f</span><span class="p">);</span>
<span class="c1">// Performs half-precision matrix multiplication: d_C_h = alpha_h * d_A_h * d_B_h + beta_h * d_C_h</span>
<span class="n">cublasHgemm</span><span class="p">(</span><span class="n">handle</span><span class="p">,</span> <span class="n">CUBLAS_OP_N</span><span class="p">,</span> <span class="n">CUBLAS_OP_N</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">alpha_h</span><span class="p">,</span> <span class="n">d_B_h</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">d_A_h</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">beta_h</span><span class="p">,</span> <span class="n">d_C_h</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
</code></pre></div></div> <p><strong>Parameter breakdown:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">handle</code>: cuBLAS library context</li> <li><code class="language-plaintext highlighter-rouge">CUBLAS_OP_N</code>: No transpose for input matrices</li> <li><code class="language-plaintext highlighter-rouge">N, M, K</code>: Matrix dimensions (output: N x M, input: N x K and K x M)</li> <li><code class="language-plaintext highlighter-rouge">&amp;alpha_h</code>: Scalar multiplier for the product</li> <li><code class="language-plaintext highlighter-rouge">d_B_h</code>: Device pointer to matrix B</li> <li><code class="language-plaintext highlighter-rouge">N</code>: Leading dimension of matrix B</li> <li><code class="language-plaintext highlighter-rouge">d_A_h</code>: Device pointer to matrix A</li> <li><code class="language-plaintext highlighter-rouge">K</code>: Leading dimension of matrix A</li> <li><code class="language-plaintext highlighter-rouge">&amp;beta_h</code>: Scalar multiplier for matrix C</li> <li><code class="language-plaintext highlighter-rouge">d_C_h</code>: Device pointer to output matrix C</li> <li><code class="language-plaintext highlighter-rouge">N</code>: Leading dimension of matrix C</li> </ul> <h4 id="cublaslt">cuBLASLt</h4> <p>Extension for more flexible APIs, mainly for deep learning workloads. Optimized for Tensor Cores, allows for more fine-grained control. Generally, is faster than cuBLAS.</p> <p>cuBLASLt provides a more flexible and high-performance matrix multiplication API, especially suited for deep learning and Tensor Core acceleration. It allows fine-grained control over operation parameters and memory layouts.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cublasLtHandle_t</span> <span class="n">handle</span><span class="p">;</span>
<span class="n">cublasLtCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">handle</span><span class="p">);</span>

<span class="n">cublasLtMatmulDesc_t</span> <span class="n">matmulDesc_fp32</span><span class="p">;</span>
<span class="n">cublasLtMatmulDescCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">matmulDesc_fp32</span><span class="p">,</span> <span class="n">CUBLAS_COMPUTE_32F</span><span class="p">,</span> <span class="n">CUDA_R_32F</span><span class="p">);</span>

<span class="c1">// Create matrix layouts for fp32 (float) data</span>
<span class="n">cublasLtMatrixLayout_t</span> <span class="n">matA_fp32</span><span class="p">,</span> <span class="n">matB_fp32</span><span class="p">,</span> <span class="n">matC_fp32</span><span class="p">;</span>
<span class="n">cublasLtMatrixLayoutCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">matA_fp32</span><span class="p">,</span> <span class="n">CUDA_R_32F</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">K</span><span class="p">);</span> <span class="c1">// A: M x K</span>
<span class="n">cublasLtMatrixLayoutCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">matB_fp32</span><span class="p">,</span> <span class="n">CUDA_R_32F</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span> <span class="c1">// B: K x N</span>
<span class="n">cublasLtMatrixLayoutCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">matC_fp32</span><span class="p">,</span> <span class="n">CUDA_R_32F</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span> <span class="c1">// C: M x N</span>

<span class="n">cublasLtMatmul</span><span class="p">(</span><span class="n">handle</span><span class="p">,</span> <span class="n">matmulDesc_fp32</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">alpha_h</span><span class="p">,</span> <span class="n">d_B_fp32</span><span class="p">,</span> <span class="n">matB_fp32</span><span class="p">,</span> <span class="n">d_A_fp32</span><span class="p">,</span> <span class="n">matA_fp32</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">beta_h</span><span class="p">,</span> <span class="n">d_C_fp32</span><span class="p">,</span> <span class="n">matC_fp32</span><span class="p">,</span> <span class="n">d_C_fp32</span><span class="p">,</span> <span class="n">matC_fp32</span><span class="p">,</span> <span class="nb">nullptr</span><span class="p">,</span> <span class="nb">nullptr</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>
</code></pre></div></div> <p>This example demonstrates cuBLASLt performing matrix multiplication in single precision (fp32). The matrix layouts, descriptors, and device pointers all use float (fp32) types for consistency.</p> <p><strong>Parameter breakdown:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">handle</code>: cuBLASLt library context</li> <li><code class="language-plaintext highlighter-rouge">matmulDesc_fp32</code>: Matmul operation descriptor (algorithm, compute type, etc.)</li> <li><code class="language-plaintext highlighter-rouge">&amp;alpha_h</code>: Scalar multiplier for the product</li> <li><code class="language-plaintext highlighter-rouge">d_B_fp32</code>: Device pointer to matrix B (float)</li> <li><code class="language-plaintext highlighter-rouge">matB_fp32</code>: Layout descriptor for matrix B (fp32)</li> <li><code class="language-plaintext highlighter-rouge">d_A_fp32</code>: Device pointer to matrix A (float)</li> <li><code class="language-plaintext highlighter-rouge">matA_fp32</code>: Layout descriptor for matrix A (fp32)</li> <li><code class="language-plaintext highlighter-rouge">&amp;beta_h</code>: Scalar multiplier for matrix C</li> <li><code class="language-plaintext highlighter-rouge">d_C_fp32</code>: Device pointer to output matrix C (float)</li> <li><code class="language-plaintext highlighter-rouge">matC_fp32</code>: Layout descriptor for matrix C (fp32)</li> <li><code class="language-plaintext highlighter-rouge">d_C_fp32</code>, <code class="language-plaintext highlighter-rouge">matC_fp32</code>: Workspace/output buffer and its layout (can be same as above)</li> <li><code class="language-plaintext highlighter-rouge">nullptr, nullptr</code>: Optional algorithm and workspace pointers (not used here)</li> <li><code class="language-plaintext highlighter-rouge">0, 0</code>: Size parameters for workspace (set to zero if not used)</li> </ul> <h4 id="cublasxt">cuBLASXt</h4> <p>Multi-GPU BLAS support. Thread-safe. Host+GPU solving is slower due to memory bottlenecks, but the GH200 chip’s fast interconnect may help.</p> <p>The following example demonstrates how to use cuBLASXt to perform single-precision matrix multiplication (SGEMM) across multiple GPUs. cuBLASXt allows you to select devices and operate directly on host memory, automatically managing data transfers and computation distribution.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cublasXtHandle_t</span> <span class="n">handle</span><span class="p">;</span>
<span class="n">CHECK_CUBLAS</span><span class="p">(</span><span class="n">cublasXtCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">handle</span><span class="p">));</span>

<span class="kt">int</span> <span class="n">devices</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">};</span>
<span class="n">cublasXtDeviceSelect</span><span class="p">(</span><span class="n">handle</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">devices</span><span class="p">);</span> <span class="c1">// Select GPU 0 for computation</span>

<span class="n">cublasXtSgemm</span><span class="p">(</span><span class="n">handle</span><span class="p">,</span> <span class="n">CUBLAS_OP_N</span><span class="p">,</span> <span class="n">CUBLAS_OP_N</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">alpha</span><span class="p">,</span> <span class="n">B_host</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">A_host</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">beta</span><span class="p">,</span> <span class="n">C_host_gpu</span><span class="p">,</span> <span class="n">N</span><span class="p">);</span>
</code></pre></div></div> <p><strong>Parameter breakdown:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">handle</code>: cuBLASXt library context</li> <li><code class="language-plaintext highlighter-rouge">CUBLAS_OP_N</code>: No transpose for input matrices</li> <li><code class="language-plaintext highlighter-rouge">N, M, K</code>: Matrix dimensions (output: N x M, input: N x K and K x M)</li> <li><code class="language-plaintext highlighter-rouge">&amp;alpha</code>: Scalar multiplier for the product</li> <li><code class="language-plaintext highlighter-rouge">B_host</code>: Pointer to matrix B in host memory</li> <li><code class="language-plaintext highlighter-rouge">N</code>: Leading dimension of matrix B</li> <li><code class="language-plaintext highlighter-rouge">A_host</code>: Pointer to matrix A in host memory</li> <li><code class="language-plaintext highlighter-rouge">K</code>: Leading dimension of matrix A</li> <li><code class="language-plaintext highlighter-rouge">&amp;beta</code>: Scalar multiplier for matrix C</li> <li><code class="language-plaintext highlighter-rouge">C_host_gpu</code>: Pointer to output matrix C (host memory, result may be computed on GPU)</li> <li><code class="language-plaintext highlighter-rouge">N</code>: Leading dimension of matrix C</li> </ul> <h4 id="cublasdx">cuBLASDx</h4> <p>Device-side BLAS API (preview) for fusing operations inside CUDA kernels, reducing latency and improving performance.</p> <h4 id="cutlass">CUTLASS</h4> <p>cuBLAS and variants run on the host, and cuBLASDx is not yet fully optimized or documented. Matrix multiplication is key for deep learning, but cuBLAS is limited in fusing custom operations. CUTLASS is a template library for flexible, efficient fusion of linear algebra operations, ideal for deep learning and custom GPU workloads.</p> <h3 id="cudnn">cuDNN</h3> <p>NVIDIA cuDNN is a GPU-accelerated library providing highly optimized implementations of core deep learning operations, including matrix multiplication (GEMM), pooling, softmax, activation functions, tensor transformations, batch normalization, and more.</p> <p>The following example demonstrates applying a tanh activation function to a 4D tensor using cuDNN:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cudnnHandle_t</span> <span class="n">cudnn</span><span class="p">;</span>
<span class="n">cudnnCreate</span><span class="p">(</span><span class="o">&amp;</span><span class="n">cudnn</span><span class="p">);</span>

<span class="c1">// Define input tensor: batch_size x channels x height x width</span>
<span class="n">cudnnTensorDescriptor_t</span> <span class="n">input_descriptor</span><span class="p">;</span>
<span class="n">cudnnCreateTensorDescriptor</span><span class="p">(</span><span class="o">&amp;</span><span class="n">input_descriptor</span><span class="p">);</span>
<span class="n">cudnnSetTensor4dDescriptor</span><span class="p">(</span><span class="n">input_descriptor</span><span class="p">,</span> <span class="n">CUDNN_TENSOR_NCHW</span><span class="p">,</span> <span class="n">CUDNN_DATA_FLOAT</span><span class="p">,</span>
                           <span class="n">batch_size</span><span class="p">,</span> <span class="n">channels</span><span class="p">,</span> <span class="n">height</span><span class="p">,</span> <span class="n">width</span><span class="p">);</span>

<span class="c1">// Configure tanh activation</span>
<span class="n">cudnnActivationDescriptor_t</span> <span class="n">activation_descriptor</span><span class="p">;</span>
<span class="n">cudnnCreateActivationDescriptor</span><span class="p">(</span><span class="o">&amp;</span><span class="n">activation_descriptor</span><span class="p">);</span>
<span class="n">cudnnSetActivationDescriptor</span><span class="p">(</span><span class="n">activation_descriptor</span><span class="p">,</span> <span class="n">CUDNN_ACTIVATION_TANH</span><span class="p">,</span>
                             <span class="n">CUDNN_PROPAGATE_NAN</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">);</span>

<span class="c1">// Apply activation: output = alpha * tanh(input) + beta * output</span>
<span class="n">cudnnActivationForward</span><span class="p">(</span><span class="n">cudnn</span><span class="p">,</span> <span class="n">activation_descriptor</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">alpha</span><span class="p">,</span> <span class="n">input_descriptor</span><span class="p">,</span> <span class="n">d_input</span><span class="p">,</span>
                       <span class="o">&amp;</span><span class="n">beta</span><span class="p">,</span> <span class="n">input_descriptor</span><span class="p">,</span> <span class="n">d_output_cudnn</span><span class="p">);</span>
</code></pre></div></div> <p>cuDNN also supports advanced fusion patterns, combining multiple operations for greater efficiency. Instead of individual API calls, you can express computation as a graph, which cuDNN optimizes for maximum performance on your hardware.</p>]]></content><author><name>Rahul Steiger</name></author><category term="CUDA"/><category term="GH200"/><category term="C++"/><summary type="html"><![CDATA[This blog contains the notes of me (properly) learning how to use CUDA.]]></summary></entry><entry><title type="html">CUDA Programming (Optimizing GEMM)</title><link href="https://rahulsteiger.github.io/blog/2025/cuda-programming_gemm/" rel="alternate" type="text/html" title="CUDA Programming (Optimizing GEMM)"/><published>2025-10-12T00:00:00+00:00</published><updated>2025-10-12T00:00:00+00:00</updated><id>https://rahulsteiger.github.io/blog/2025/cuda-programming_gemm</id><content type="html" xml:base="https://rahulsteiger.github.io/blog/2025/cuda-programming_gemm/"><![CDATA[<p>These are my notes for Chapter 7 of the CUDA Programming Course – High-Performance Computing with GPUs on <a href="https://www.youtube.com/watch?v=86FAWCzIe_4&amp;t">YouTube</a>. For all experiments, I will be working on the ALPS cluster, which features a GH200 System.</p> <p>This part of the course covers building a fast CUDA SGEMM from scratch. The approach is based on this <a href="https://siboehm.com/articles/22/CUDA-MMM">blog</a> post, and the associated code can be found on <a href="https://github.com/siboehm/SGEMM_CUDA?tab=readme-ov-file">GitHub</a>. The original blog is very well written and features excellent figures, so I recommend checking it out before/ while reading my notes.</p> <p>Since I am working with the GH200, I can do some Hopper specific optimizations that are not mentioned in the course or original blog post. But that is an ambitious goal.</p> <p>This is still a work in progress.</p> <h2 id="setup">Setup</h2> <p>I will use the testing infrastructure from the original blog post’s <a href="https://github.com/siboehm/SGEMM_CUDA?tab=readme-ov-file">GitHub</a> repository. The only change I need to make is setting the <code class="language-plaintext highlighter-rouge">cmake</code> <code class="language-plaintext highlighter-rouge">CUDA_COMPUTE_CAPABILITY</code> parameter to 90.</p> <h2 id="baseline-implementation">Baseline implementation</h2> <p>The baseline kernel that we will be improving upon looks as follows:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">sgemm_baseline</span><span class="p">(</span><span class="kt">int</span> <span class="n">M</span><span class="p">,</span> <span class="kt">int</span> <span class="n">N</span><span class="p">,</span> <span class="kt">int</span> <span class="n">K</span><span class="p">,</span> <span class="kt">float</span> <span class="n">alpha</span><span class="p">,</span> <span class="k">const</span> <span class="kt">float</span> <span class="o">*</span><span class="n">A</span><span class="p">,</span>
                            <span class="k">const</span> <span class="kt">float</span> <span class="o">*</span><span class="n">B</span><span class="p">,</span> <span class="kt">float</span> <span class="n">beta</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">C</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">const</span> <span class="n">uint</span> <span class="n">x</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
  <span class="k">const</span> <span class="n">uint</span> <span class="n">y</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">y</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">y</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>

  <span class="c1">// if statement is necessary to make things work under tile quantization</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">x</span> <span class="o">&lt;</span> <span class="n">M</span> <span class="o">&amp;&amp;</span> <span class="n">y</span> <span class="o">&lt;</span> <span class="n">N</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">tmp</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">tmp</span> <span class="o">+=</span> <span class="n">A</span><span class="p">[</span><span class="n">x</span> <span class="o">*</span> <span class="n">K</span> <span class="o">+</span> <span class="n">i</span><span class="p">]</span> <span class="o">*</span> <span class="n">B</span><span class="p">[</span><span class="n">i</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">y</span><span class="p">];</span>
    <span class="p">}</span>
    <span class="c1">// C = α*(A@B)+β*C</span>
    <span class="n">C</span><span class="p">[</span><span class="n">x</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">y</span><span class="p">]</span> <span class="o">=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">tmp</span> <span class="o">+</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">C</span><span class="p">[</span><span class="n">x</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">y</span><span class="p">];</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>The kernel is launched with:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// create as many blocks as necessary to map all of C</span>
<span class="kt">int</span> <span class="n">BLOCK_SIZE</span> <span class="o">=</span> <span class="mi">32</span>
<span class="n">dim3</span> <span class="nf">gridDim</span><span class="p">(</span><span class="n">CEIL_DIV</span><span class="p">(</span><span class="n">M</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="p">),</span> <span class="n">CEIL_DIV</span><span class="p">(</span><span class="n">N</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="p">),</span> <span class="mi">1</span><span class="p">);</span>
<span class="c1">// 32 * 32 = 1024 thread per block</span>
<span class="n">dim3</span> <span class="nf">blockDim</span><span class="p">(</span><span class="n">BLOCK_SIZE</span><span class="p">,</span> <span class="n">BLOCK_SIZE</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
<span class="c1">// launch the asynchronous execution of the kernel on the device</span>
<span class="c1">// The function call returns immediately on the host</span>
<span class="n">sgemm_baseline</span><span class="o">&lt;&lt;&lt;</span><span class="n">gridDim</span><span class="p">,</span> <span class="n">blockDim</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">M</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">alpha</span><span class="p">,</span> <span class="n">A</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">beta</span><span class="p">,</span> <span class="n">C</span><span class="p">);</span>
</code></pre></div></div> <p>For $m=n=k=4096$, this kernel achieves 502.5 GFLOPs. In comparison, cuBLAS achieves 50058.0 GFLOPs for the same matrix sizes, which is nearly 100x faster than the baseline implementation.</p> <h2 id="global-memory-coalescing">Global Memory Coalescing</h2> <p>Looking at how we are launching the kernel and assuming $m=n=k=4096$, the warps will be executed with the following threads:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Warp 0: <span class="o">(</span><span class="nv">y</span><span class="o">=</span>0, <span class="nv">x</span><span class="o">=</span>0..31<span class="o">)</span>
Warp 1: <span class="o">(</span><span class="nv">y</span><span class="o">=</span>0, <span class="nv">x</span><span class="o">=</span>32..63<span class="o">)</span>
...
</code></pre></div></div> <p>Consequently, each thread in a warp accesses a different row of A, the same column of B, and a different entry of C (along the column dimension). GPUs support 128B memory operations on contiguous data. Such load instructions can be issued not only on data used by a single thread, but on the entire data used during warp execution. Assuming each thread in a warp loads a different 32-bit float and that data is stored in contiguous memory, four of them can be coalesced into a single transaction.</p> <p>Since the matrices are stored in row-major order, we can take advantage of 128B loading for matrix A. However, since we access non-contiguous memory in matrices B and C, we cannot exploit this optimization. The solution is to redefine <code class="language-plaintext highlighter-rouge">x</code> and <code class="language-plaintext highlighter-rouge">y</code> to ensure that warps access data that can be coalesced.</p> <p>We can achieve this as follows:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const int x = blockIdx.x * BLOCKSIZE + (threadIdx.x / BLOCKSIZE);
const int y = blockIdx.y * BLOCKSIZE + (threadIdx.x % BLOCKSIZE);
</code></pre></div></div> <p>This ensures that each thread within a warp accesses a consecutive set of 32 columns of B and the same consecutive entries of C. Consequently, we reduce the number of load transactions each warp needs to perform while keeping the total number of warps unchanged.</p> <p>For $m=n=k=4096$, this kernel achieves 6352.2 GFLOPs, a nearly 12x improvement, but still 8x slower than the cuBlas implementation.</p> <h2 id="shared-memory-cache-blocking">Shared Memory Cache-Blocking</h2> <p>Each block is executed on a Streaming Multiprocessor (SM). Each SM has some (very fast) shared memory that can be accessed by all threads within the same block. Every block computes a $32 \times 32$ block of C. For this, each block uses 32 consecutive rows of A and 32 consecutive columns of B. However, these rows and columns are currently reloaded by every single warp.</p> <p>We will move a $32 \times 32$ chunk of A and B into shared memory and let each warp compute its part before continuing to the next chunks.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span> <span class="o">&lt;</span><span class="k">const</span> <span class="kt">int</span> <span class="n">BLOCKSIZE</span><span class="p">&gt;</span>
<span class="n">__global__</span> <span class="kt">void</span> <span class="nf">sgemm_shared_mem_block</span><span class="p">(</span><span class="kt">int</span> <span class="n">M</span><span class="p">,</span> <span class="kt">int</span> <span class="n">N</span><span class="p">,</span> <span class="kt">int</span> <span class="n">K</span><span class="p">,</span> <span class="kt">float</span> <span class="n">alpha</span><span class="p">,</span>
                                       <span class="k">const</span> <span class="kt">float</span> <span class="o">*</span><span class="n">A</span><span class="p">,</span> <span class="k">const</span> <span class="kt">float</span> <span class="o">*</span><span class="n">B</span><span class="p">,</span>
                                       <span class="kt">float</span> <span class="n">beta</span><span class="p">,</span> <span class="kt">float</span> <span class="o">*</span><span class="n">C</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// the output block that we want to compute in this threadblock</span>
  <span class="k">const</span> <span class="n">uint</span> <span class="n">cRow</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
  <span class="k">const</span> <span class="n">uint</span> <span class="n">cCol</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>

  <span class="c1">// allocate buffer for current block in fast shared mem</span>
  <span class="c1">// shared mem is shared between all threads in a block</span>
  <span class="n">__shared__</span> <span class="kt">float</span> <span class="n">As</span><span class="p">[</span><span class="n">BLOCKSIZE</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span><span class="p">];</span>
  <span class="n">__shared__</span> <span class="kt">float</span> <span class="n">Bs</span><span class="p">[</span><span class="n">BLOCKSIZE</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span><span class="p">];</span>

  <span class="c1">// the inner row &amp; col that we're accessing in this thread</span>
  <span class="k">const</span> <span class="n">uint</span> <span class="n">threadCol</span> <span class="o">=</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">%</span> <span class="n">BLOCKSIZE</span><span class="p">;</span>
  <span class="k">const</span> <span class="n">uint</span> <span class="n">threadRow</span> <span class="o">=</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">/</span> <span class="n">BLOCKSIZE</span><span class="p">;</span>

  <span class="c1">// advance pointers to the starting positions</span>
  <span class="n">A</span> <span class="o">+=</span> <span class="n">cRow</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span> <span class="o">*</span> <span class="n">K</span><span class="p">;</span>                    <span class="c1">// row=cRow, col=0</span>
  <span class="n">B</span> <span class="o">+=</span> <span class="n">cCol</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span><span class="p">;</span>                        <span class="c1">// row=0, col=cCol</span>
  <span class="n">C</span> <span class="o">+=</span> <span class="n">cRow</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">cCol</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span><span class="p">;</span> <span class="c1">// row=cRow, col=cCol</span>

  <span class="kt">float</span> <span class="n">tmp</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">bkIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">+=</span> <span class="n">BLOCKSIZE</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Have each thread load one of the elements in A &amp; B</span>
    <span class="c1">// Make the threadCol (=threadIdx.x) the consecutive index</span>
    <span class="c1">// to allow global memory access coalescing</span>
    <span class="n">As</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">]</span> <span class="o">=</span> <span class="n">A</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">K</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">];</span>
    <span class="n">Bs</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">]</span> <span class="o">=</span> <span class="n">B</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">];</span>

    <span class="c1">// block threads in this block until cache is fully populated</span>
    <span class="n">__syncthreads</span><span class="p">();</span>
    <span class="n">A</span> <span class="o">+=</span> <span class="n">BLOCKSIZE</span><span class="p">;</span>
    <span class="n">B</span> <span class="o">+=</span> <span class="n">BLOCKSIZE</span> <span class="o">*</span> <span class="n">N</span><span class="p">;</span>

    <span class="c1">// execute the dotproduct on the currently cached block</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">dotIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">dotIdx</span> <span class="o">&lt;</span> <span class="n">BLOCKSIZE</span><span class="p">;</span> <span class="o">++</span><span class="n">dotIdx</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">tmp</span> <span class="o">+=</span> <span class="n">As</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span> <span class="o">+</span> <span class="n">dotIdx</span><span class="p">]</span> <span class="o">*</span>
             <span class="n">Bs</span><span class="p">[</span><span class="n">dotIdx</span> <span class="o">*</span> <span class="n">BLOCKSIZE</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">];</span>
    <span class="p">}</span>
    <span class="c1">// need to sync again at the end, to avoid faster threads</span>
    <span class="c1">// fetching the next block into the cache before slower threads are done</span>
    <span class="n">__syncthreads</span><span class="p">();</span>
  <span class="p">}</span>
  <span class="n">C</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">]</span> <span class="o">=</span>
      <span class="n">alpha</span> <span class="o">*</span> <span class="n">tmp</span> <span class="o">+</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">C</span><span class="p">[</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">];</span>
<span class="p">}</span>
</code></pre></div></div> <p>For $m=n=k=4096$, this kernel achieves 9174.1 GFLOPs, a 1.5x improvement over the previous version, but still more than 5x slower than the cuBLAS implementation.</p> <h2 id="1d-blocktiling">1D Blocktiling</h2> <p>Instead of computing a single entry per thread, we now compute multiple entries of C per thread. This increases arithmetic intensity since a single load can be reused to compute multiple results.</p> <p>We introduce new parameters: BM and BN specify the block dimensions (rows and columns of C per block), BK controls the shared memory tile size (i.e. how much shared memory we can load in 1 iteration given the number of threads available to us), and TM defines how many results each thread computes.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// allocate thread-local cache for results in registerfile</span>

<span class="n">__shared__</span> <span class="kt">float</span> <span class="n">As</span><span class="p">[</span><span class="n">BM</span> <span class="o">*</span> <span class="n">BK</span><span class="p">];</span>
<span class="n">__shared__</span> <span class="kt">float</span> <span class="n">Bs</span><span class="p">[</span><span class="n">BK</span> <span class="o">*</span> <span class="n">BN</span><span class="p">];</span>
<span class="kt">float</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">TM</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mf">0.0</span><span class="p">};</span>

<span class="c1">// outer loop over block tiles</span>
<span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">bkIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">+=</span> <span class="n">BK</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// populate the SMEM caches</span>
  <span class="n">As</span><span class="p">[</span><span class="n">innerRowA</span> <span class="o">*</span> <span class="n">BK</span> <span class="o">+</span> <span class="n">innerColA</span><span class="p">]</span> <span class="o">=</span> <span class="n">A</span><span class="p">[</span><span class="n">innerRowA</span> <span class="o">*</span> <span class="n">K</span> <span class="o">+</span> <span class="n">innerColA</span><span class="p">];</span>
  <span class="n">Bs</span><span class="p">[</span><span class="n">innerRowB</span> <span class="o">*</span> <span class="n">BN</span> <span class="o">+</span> <span class="n">innerColB</span><span class="p">]</span> <span class="o">=</span> <span class="n">B</span><span class="p">[</span><span class="n">innerRowB</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">innerColB</span><span class="p">];</span>
  <span class="n">__syncthreads</span><span class="p">();</span>

  <span class="c1">// advance blocktile</span>
  <span class="n">A</span> <span class="o">+=</span> <span class="n">BK</span><span class="p">;</span>
  <span class="n">B</span> <span class="o">+=</span> <span class="n">BK</span> <span class="o">*</span> <span class="n">N</span><span class="p">;</span>

  <span class="c1">// calculate per-thread results</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">dotIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">dotIdx</span> <span class="o">&lt;</span> <span class="n">BK</span><span class="p">;</span> <span class="o">++</span><span class="n">dotIdx</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// we make the dot product loop the outer loop, which facilitates</span>
    <span class="c1">// reuse of the Bs entry, which we can cache in a temporary variable</span>
    <span class="kt">float</span> <span class="n">tmpB</span> <span class="o">=</span> <span class="n">Bs</span><span class="p">[</span><span class="n">dotIdx</span> <span class="o">*</span> <span class="n">BN</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">];</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdx</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="o">++</span><span class="n">resIdx</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdx</span><span class="p">]</span> <span class="o">+=</span>
          <span class="n">As</span><span class="p">[(</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdx</span><span class="p">)</span> <span class="o">*</span> <span class="n">BK</span> <span class="o">+</span> <span class="n">dotIdx</span><span class="p">]</span> <span class="o">*</span> <span class="n">tmpB</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="n">__syncthreads</span><span class="p">();</span>
<span class="p">}</span>

<span class="c1">// write out the results</span>
<span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdx</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="o">++</span><span class="n">resIdx</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">C</span><span class="p">[(</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdx</span><span class="p">)</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">]</span> <span class="o">=</span>
      <span class="n">alpha</span> <span class="o">*</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdx</span><span class="p">]</span> <span class="o">+</span>
      <span class="n">beta</span> <span class="o">*</span> <span class="n">C</span><span class="p">[(</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdx</span><span class="p">)</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span><span class="p">];</span>
<span class="p">}</span>
</code></pre></div></div> <p>The computation loop makes the dot product the outer loop, allowing us to cache entries from B in a temporary variable and reuse them across multiple accumulations.</p> <p>For $m=n=k=4096$, this kernel achieves 17040.9 GFLOPs, a 1.85x improvement over the previous version, but still nearly 3x slower than the cuBLAS implementation.</p> <h2 id="2d-blocktiling">2D Blocktiling</h2> <p>Instead of computing a single <code class="language-plaintext highlighter-rouge">TM</code> row of <code class="language-plaintext highlighter-rouge">C</code>, we compute a <code class="language-plaintext highlighter-rouge">TM</code> $\times$ <code class="language-plaintext highlighter-rouge">TN</code> block of <code class="language-plaintext highlighter-rouge">C</code> per thread. This further increases the arithmetic intensity. Since each thread now computes more results, it must load multiple items from both A and B.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Each block computes BM * BN entries of C</span>
<span class="k">const</span> <span class="n">uint</span> <span class="n">totalResultsBlocktile</span> <span class="o">=</span> <span class="n">BM</span> <span class="o">*</span> <span class="n">BN</span><span class="p">;</span>

<span class="c1">// Each thread computes TM * TN entries of C -&gt; divide by that to get the number of threads per tile</span>
<span class="k">const</span> <span class="n">uint</span> <span class="n">numThreadsBlocktile</span> <span class="o">=</span> <span class="n">totalResultsBlocktile</span> <span class="o">/</span> <span class="p">(</span><span class="n">TM</span> <span class="o">*</span> <span class="n">TN</span><span class="p">);</span>

<span class="c1">// strideA and strideB basically represent how many entries of A and B all threads can load in 1 iteration</span>
<span class="k">const</span> <span class="n">uint</span> <span class="n">strideA</span> <span class="o">=</span> <span class="n">numThreadsBlocktile</span> <span class="o">/</span> <span class="n">BK</span><span class="p">;</span>
<span class="k">const</span> <span class="n">uint</span> <span class="n">strideB</span> <span class="o">=</span> <span class="n">numThreadsBlocktile</span> <span class="o">/</span> <span class="n">BN</span><span class="p">;</span>

<span class="c1">// outer-most loop over block tiles</span>
<span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">bkIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">+=</span> <span class="n">BK</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// populate the SMEM caches</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">loadOffset</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">loadOffset</span> <span class="o">&lt;</span> <span class="n">BM</span><span class="p">;</span> <span class="n">loadOffset</span> <span class="o">+=</span> <span class="n">strideA</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">As</span><span class="p">[(</span><span class="n">innerRowA</span> <span class="o">+</span> <span class="n">loadOffset</span><span class="p">)</span> <span class="o">*</span> <span class="n">BK</span> <span class="o">+</span> <span class="n">innerColA</span><span class="p">]</span> <span class="o">=</span>
        <span class="n">A</span><span class="p">[(</span><span class="n">innerRowA</span> <span class="o">+</span> <span class="n">loadOffset</span><span class="p">)</span> <span class="o">*</span> <span class="n">K</span> <span class="o">+</span> <span class="n">innerColA</span><span class="p">];</span>
  <span class="p">}</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">loadOffset</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">loadOffset</span> <span class="o">&lt;</span> <span class="n">BK</span><span class="p">;</span> <span class="n">loadOffset</span> <span class="o">+=</span> <span class="n">strideB</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">Bs</span><span class="p">[(</span><span class="n">innerRowB</span> <span class="o">+</span> <span class="n">loadOffset</span><span class="p">)</span> <span class="o">*</span> <span class="n">BN</span> <span class="o">+</span> <span class="n">innerColB</span><span class="p">]</span> <span class="o">=</span>
        <span class="n">B</span><span class="p">[(</span><span class="n">innerRowB</span> <span class="o">+</span> <span class="n">loadOffset</span><span class="p">)</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">innerColB</span><span class="p">];</span>
  <span class="p">}</span>
  <span class="n">__syncthreads</span><span class="p">();</span>
  <span class="p">...</span>
<span class="p">}</span>
</code></pre></div></div> <p>To compute the <code class="language-plaintext highlighter-rouge">TM</code> $\times$ <code class="language-plaintext highlighter-rouge">TN</code> block, each thread repeatedly accesses the same entries of A and B from shared memory. To reduce these expensive shared memory accesses, we cache the entries in local registers.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// register caches for As and Bs</span>
<span class="kt">float</span> <span class="n">regM</span><span class="p">[</span><span class="n">TM</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mf">0.0</span><span class="p">};</span>
<span class="kt">float</span> <span class="n">regN</span><span class="p">[</span><span class="n">TN</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mf">0.0</span><span class="p">};</span>
<span class="kt">float</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">TM</span> <span class="o">*</span> <span class="n">TN</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mf">0.0</span><span class="p">};</span>

<span class="c1">// outer-most loop over block tiles</span>
<span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">bkIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="n">bkIdx</span> <span class="o">+=</span> <span class="n">BK</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// Load Memory from before</span>
  <span class="p">...</span>
  <span class="c1">// advance blocktile</span>
    <span class="n">A</span> <span class="o">+=</span> <span class="n">BK</span><span class="p">;</span>     <span class="c1">// move BK columns to right</span>
    <span class="n">B</span> <span class="o">+=</span> <span class="n">BK</span> <span class="o">*</span> <span class="n">N</span><span class="p">;</span> <span class="c1">// move BK rows down</span>

    <span class="c1">// calculate per-thread results</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">dotIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">dotIdx</span> <span class="o">&lt;</span> <span class="n">BK</span><span class="p">;</span> <span class="o">++</span><span class="n">dotIdx</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// block into registers</span>
      <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">regM</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">As</span><span class="p">[(</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">i</span><span class="p">)</span> <span class="o">*</span> <span class="n">BK</span> <span class="o">+</span> <span class="n">dotIdx</span><span class="p">];</span>
      <span class="p">}</span>
      <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">TN</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">regN</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">Bs</span><span class="p">[</span><span class="n">dotIdx</span> <span class="o">*</span> <span class="n">BN</span> <span class="o">+</span> <span class="n">threadCol</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
      <span class="p">}</span>
      <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdxM</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdxM</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="o">++</span><span class="n">resIdxM</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdxN</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdxN</span> <span class="o">&lt;</span> <span class="n">TN</span><span class="p">;</span> <span class="o">++</span><span class="n">resIdxN</span><span class="p">)</span> <span class="p">{</span>
          <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdxM</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span><span class="p">]</span> <span class="o">+=</span>
              <span class="n">regM</span><span class="p">[</span><span class="n">resIdxM</span><span class="p">]</span> <span class="o">*</span> <span class="n">regN</span><span class="p">[</span><span class="n">resIdxN</span><span class="p">];</span>
        <span class="p">}</span>
      <span class="p">}</span>
    <span class="p">}</span>
    <span class="n">__syncthreads</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div> <p>For $m=n=k=4096$, this kernel achieves 25933.0 GFLOPs. We are halfway to achieving the performance of the cuBLAS implementation.</p> <h2 id="vectorization">Vectorization</h2> <p>We replace scalar loads and stores from global memory with vectorized operations using <code class="language-plaintext highlighter-rouge">float4</code>. This allows the GPU to issue fewer memory transactions by loading/storing 4 floats per instruction instead of 1.</p> <p>For the tile loading phase, each thread now loads 4 consecutive elements at once:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Update population of memory caches</span>
<span class="n">float4</span> <span class="n">tmp</span> <span class="o">=</span>
    <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">float4</span> <span class="o">*&gt;</span><span class="p">(</span><span class="o">&amp;</span><span class="n">A</span><span class="p">[</span><span class="n">innerRowA</span> <span class="o">*</span> <span class="n">K</span> <span class="o">+</span> <span class="n">innerColA</span> <span class="o">*</span> <span class="mi">4</span><span class="p">])[</span><span class="mi">0</span><span class="p">];</span>
<span class="n">As</span><span class="p">[(</span><span class="n">innerColA</span> <span class="o">*</span> <span class="mi">4</span> <span class="o">+</span> <span class="mi">0</span><span class="p">)</span> <span class="o">*</span> <span class="n">BM</span> <span class="o">+</span> <span class="n">innerRowA</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
<span class="n">As</span><span class="p">[(</span><span class="n">innerColA</span> <span class="o">*</span> <span class="mi">4</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">BM</span> <span class="o">+</span> <span class="n">innerRowA</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>
<span class="n">As</span><span class="p">[(</span><span class="n">innerColA</span> <span class="o">*</span> <span class="mi">4</span> <span class="o">+</span> <span class="mi">2</span><span class="p">)</span> <span class="o">*</span> <span class="n">BM</span> <span class="o">+</span> <span class="n">innerRowA</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp</span><span class="p">.</span><span class="n">z</span><span class="p">;</span>
<span class="n">As</span><span class="p">[(</span><span class="n">innerColA</span> <span class="o">*</span> <span class="mi">4</span> <span class="o">+</span> <span class="mi">3</span><span class="p">)</span> <span class="o">*</span> <span class="n">BM</span> <span class="o">+</span> <span class="n">innerRowA</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp</span><span class="p">.</span><span class="n">w</span><span class="p">;</span>

<span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">float4</span> <span class="o">*&gt;</span><span class="p">(</span><span class="o">&amp;</span><span class="n">Bs</span><span class="p">[</span><span class="n">innerRowB</span> <span class="o">*</span> <span class="n">BN</span> <span class="o">+</span> <span class="n">innerColB</span> <span class="o">*</span> <span class="mi">4</span><span class="p">])[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span>
    <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">float4</span> <span class="o">*&gt;</span><span class="p">(</span><span class="o">&amp;</span><span class="n">B</span><span class="p">[</span><span class="n">innerRowB</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">innerColB</span> <span class="o">*</span> <span class="mi">4</span><span class="p">])[</span><span class="mi">0</span><span class="p">];</span>
</code></pre></div></div> <p>We apply the same vectorization when writing results back to C:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdxM</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdxM</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="n">resIdxM</span> <span class="o">+=</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdxN</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdxN</span> <span class="o">&lt;</span> <span class="n">TN</span><span class="p">;</span> <span class="n">resIdxN</span> <span class="o">+=</span> <span class="mi">4</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// load C vector into registers</span>
    <span class="n">float4</span> <span class="n">tmp</span> <span class="o">=</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">float4</span> <span class="o">*&gt;</span><span class="p">(</span>
        <span class="o">&amp;</span><span class="n">C</span><span class="p">[(</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdxM</span><span class="p">)</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span><span class="p">])[</span><span class="mi">0</span><span class="p">];</span>
    <span class="c1">// perform GEMM update in reg</span>
    <span class="n">tmp</span><span class="p">.</span><span class="n">x</span> <span class="o">=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdxM</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span><span class="p">]</span> <span class="o">+</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">tmp</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="n">tmp</span><span class="p">.</span><span class="n">y</span> <span class="o">=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdxM</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span> <span class="o">+</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">tmp</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>
    <span class="n">tmp</span><span class="p">.</span><span class="n">z</span> <span class="o">=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdxM</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span> <span class="o">+</span> <span class="mi">2</span><span class="p">]</span> <span class="o">+</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">tmp</span><span class="p">.</span><span class="n">z</span><span class="p">;</span>
    <span class="n">tmp</span><span class="p">.</span><span class="n">w</span> <span class="o">=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">threadResults</span><span class="p">[</span><span class="n">resIdxM</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span> <span class="o">+</span> <span class="mi">3</span><span class="p">]</span> <span class="o">+</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">tmp</span><span class="p">.</span><span class="n">w</span><span class="p">;</span>
    <span class="c1">// write back</span>
    <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">float4</span> <span class="o">*&gt;</span><span class="p">(</span>
        <span class="o">&amp;</span><span class="n">C</span><span class="p">[(</span><span class="n">threadRow</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdxM</span><span class="p">)</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">threadCol</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span><span class="p">])[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span>
        <span class="n">tmp</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>For $m=n=k=4096$, this kernel achieves 31420.8 GFLOPs. We are getting closer…</p> <h2 id="warptiling">Warptiling</h2> <p>A warp consists of 32 threads and is the fundamental unit of scheduling on NVIDIA GPUs. Instead of having each thread independently compute its portion of the output matrix, warptiling assigns a tile of the output matrix to an entire warp. Threads within a warp collaborate to compute this tile, which reduces shared memory bank conflicts and improves data reuse.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// dotIdx loops over contents of SMEM</span>
<span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">dotIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">dotIdx</span> <span class="o">&lt;</span> <span class="n">BK</span><span class="p">;</span> <span class="o">++</span><span class="n">dotIdx</span><span class="p">)</span> <span class="p">{</span>
  <span class="c1">// populate registers for this thread's part of the warptile</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">wSubRowIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">wSubRowIdx</span> <span class="o">&lt;</span> <span class="n">WMITER</span><span class="p">;</span> <span class="o">++</span><span class="n">wSubRowIdx</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">regM</span><span class="p">[</span><span class="n">wSubRowIdx</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">i</span><span class="p">]</span> <span class="o">=</span>
          <span class="n">As</span><span class="p">[(</span><span class="n">dotIdx</span> <span class="o">*</span> <span class="n">BM</span><span class="p">)</span> <span class="o">+</span> <span class="n">warpRow</span> <span class="o">*</span> <span class="n">WM</span> <span class="o">+</span> <span class="n">wSubRowIdx</span> <span class="o">*</span> <span class="n">WSUBM</span> <span class="o">+</span>
             <span class="n">threadRowInWarp</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">wSubColIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">wSubColIdx</span> <span class="o">&lt;</span> <span class="n">WNITER</span><span class="p">;</span> <span class="o">++</span><span class="n">wSubColIdx</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">TN</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">regN</span><span class="p">[</span><span class="n">wSubColIdx</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">i</span><span class="p">]</span> <span class="o">=</span>
          <span class="n">Bs</span><span class="p">[(</span><span class="n">dotIdx</span> <span class="o">*</span> <span class="n">BN</span><span class="p">)</span> <span class="o">+</span> <span class="n">warpCol</span> <span class="o">*</span> <span class="n">WN</span> <span class="o">+</span> <span class="n">wSubColIdx</span> <span class="o">*</span> <span class="n">WSUBN</span> <span class="o">+</span>
             <span class="n">threadColInWarp</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">i</span><span class="p">];</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="c1">// execute warptile matmul. Later this will map well to</span>
  <span class="c1">// warp-wide matrix instructions, executed on tensor cores.</span>
  <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">wSubRowIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">wSubRowIdx</span> <span class="o">&lt;</span> <span class="n">WMITER</span><span class="p">;</span> <span class="o">++</span><span class="n">wSubRowIdx</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">wSubColIdx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">wSubColIdx</span> <span class="o">&lt;</span> <span class="n">WNITER</span><span class="p">;</span> <span class="o">++</span><span class="n">wSubColIdx</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// calculate per-thread results with register-cache locality</span>
      <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdxM</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdxM</span> <span class="o">&lt;</span> <span class="n">TM</span><span class="p">;</span> <span class="o">++</span><span class="n">resIdxM</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="n">uint</span> <span class="n">resIdxN</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">resIdxN</span> <span class="o">&lt;</span> <span class="n">TN</span><span class="p">;</span> <span class="o">++</span><span class="n">resIdxN</span><span class="p">)</span> <span class="p">{</span>
          <span class="n">threadResults</span><span class="p">[(</span><span class="n">wSubRowIdx</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdxM</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span><span class="n">WNITER</span> <span class="o">*</span> <span class="n">TN</span><span class="p">)</span> <span class="o">+</span>
                        <span class="p">(</span><span class="n">wSubColIdx</span> <span class="o">*</span> <span class="n">TN</span><span class="p">)</span> <span class="o">+</span> <span class="n">resIdxN</span><span class="p">]</span> <span class="o">+=</span>
              <span class="n">regM</span><span class="p">[</span><span class="n">wSubRowIdx</span> <span class="o">*</span> <span class="n">TM</span> <span class="o">+</span> <span class="n">resIdxM</span><span class="p">]</span> <span class="o">*</span>
              <span class="n">regN</span><span class="p">[</span><span class="n">wSubColIdx</span> <span class="o">*</span> <span class="n">TN</span> <span class="o">+</span> <span class="n">resIdxN</span><span class="p">];</span>
        <span class="p">}</span>
      <span class="p">}</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>This kernel achieves 41496 GFLOPs, we achieved nearly $84$% of cuBLAS.</p> <p>TODO.</p>]]></content><author><name>Rahul Steiger</name></author><category term="CUDA"/><category term="GH200"/><category term="C++"/><summary type="html"><![CDATA[This blog contains the notes of me (attempting) to create a fast GEMM CUDA implementation.]]></summary></entry><entry><title type="html">Running DeepSeek R1 locally</title><link href="https://rahulsteiger.github.io/blog/2025/llamacpp-deepseek/" rel="alternate" type="text/html" title="Running DeepSeek R1 locally"/><published>2025-02-10T00:00:00+00:00</published><updated>2025-02-10T00:00:00+00:00</updated><id>https://rahulsteiger.github.io/blog/2025/llamacpp-deepseek</id><content type="html" xml:base="https://rahulsteiger.github.io/blog/2025/llamacpp-deepseek/"><![CDATA[<p>Running these LLMs requires significant computational resources. Fortunately, I have access to HPC hardware thanks to my previous participation in Team RACKlette during my bachelor’s. The node I am using for this experiment has 2 <code class="language-plaintext highlighter-rouge">AMD EPYC 7773X 64-Core</code> CPUs, 4 <code class="language-plaintext highlighter-rouge">NVIDIA A100 80GB</code> GPUs, and <code class="language-plaintext highlighter-rouge">1 TB</code> of memory. This is the only reason I am able to play around with LLMs.</p> <h2 id="llamacpp-setup">llama.cpp setup</h2> <p>The main goal of llama.cpp is to enable LLM inference with minimal setup and state-of-the-art performance on a wide range of hardware. More information can be found <a href="https://github.com/ggerganov/llama.cpp">here</a>.</p> <p>Clone the github repository:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com:ggerganov/llama.cpp.git <span class="o">&amp;&amp;</span> <span class="nb">cd </span>llama.cpp
</code></pre></div></div> <p>Build llama.cpp:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cmake <span class="nt">-B</span> build <span class="nt">-DGGML_NATIVE</span><span class="o">=</span>OFF <span class="nt">-DGGML_CUDA</span><span class="o">=</span>ON <span class="nt">-DLLAMA_CURL</span><span class="o">=</span>ON <span class="nt">-DCMAKE_EXE_LINKER_FLAGS</span><span class="o">=</span><span class="nt">-Wl</span>,--allow-shlib-undefined <span class="nb">.</span>
cmake <span class="nt">--build</span> build <span class="nt">--config</span> Release <span class="nt">-j</span>
</code></pre></div></div> <p><strong>Note:</strong> Make sure that the cuda compiler is available (you can check it with <code class="language-plaintext highlighter-rouge">nvcc --version</code>). More information on how to install CUDA can be found <a href="https://docs.nvidia.com/cuda/cuda-installation-guide-linux/">here</a>.</p> <h2 id="downloading-the-model-from-hugging-face">Downloading the model from Hugging Face</h2> <p>As an initial step, we will be running a model with 1.58 quantization since it has the lowest hardware requirements.</p> <p>Install the Hugging Face CLI:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install</span> <span class="nt">-U</span> <span class="s2">"huggingface_hub[cli,hf_transfer]"</span>
</code></pre></div></div> <p>Download the GGUF model files from the unsloth/DeepSeek-R1-GGUF repository:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">MODEL</span><span class="o">=</span><span class="s2">"unsloth/DeepSeek-R1-GGUF"</span>

<span class="nb">export </span><span class="nv">FILES</span><span class="o">=(</span>
  <span class="s2">"DeepSeek-R1-UD-IQ1_S/DeepSeek-R1-UD-IQ1_S-00001-of-00003.gguf"</span>
  <span class="s2">"DeepSeek-R1-UD-IQ1_S/DeepSeek-R1-UD-IQ1_S-00002-of-00003.gguf"</span>
  <span class="s2">"DeepSeek-R1-UD-IQ1_S/DeepSeek-R1-UD-IQ1_S-00003-of-00003.gguf"</span>
<span class="o">)</span>

<span class="nb">export </span><span class="nv">LOCAL_DIR</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/models"</span>

<span class="nv">HF_HUB_ENABLE_HF_TRANSFER</span><span class="o">=</span>1 huggingface-cli download <span class="se">\</span>
  <span class="nt">--repo-type</span> <span class="s2">"model"</span> <span class="se">\</span>
  <span class="nt">--local-dir</span> <span class="s2">"</span><span class="nv">$LOCAL_DIR</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nv">$MODEL</span> <span class="s2">"</span><span class="k">${</span><span class="nv">FILES</span><span class="p">[@]</span><span class="k">}</span><span class="s2">"</span>
</code></pre></div></div> <h2 id="running-the-model">Running the model</h2> <p>In the same terminal session as before, we can go to the go to the directory that contains the llama-server binary:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>build/bin
</code></pre></div></div> <p>We can then start the inference server with the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./llama-server <span class="se">\</span>
  <span class="nt">--port</span> 8192 <span class="se">\</span>
  <span class="nt">--model</span> <span class="s2">"</span><span class="nv">$LOCAL_DIR</span><span class="s2">/</span><span class="k">${</span><span class="nv">FILES</span><span class="p">[0]</span><span class="k">}</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nt">--n-gpu-layers</span> 256 <span class="se">\</span>
  <span class="nt">--tensor-split</span> 24,25,25,25 <span class="se">\</span>
  <span class="nt">--split-mode</span> row <span class="se">\</span>
  <span class="nt">--flash-attn</span> <span class="se">\</span>
  <span class="nt">--ctx-size</span> 16384
</code></pre></div></div> <p>Parameter explanation:</p> <ul> <li><code class="language-plaintext highlighter-rouge">--model "$LOCAL_DIR/${FILES[0]}"</code>: Path to the first GGUF file.</li> <li><code class="language-plaintext highlighter-rouge">--tensor-split 24,25,25,25</code>: Fraction of the model that is split across the GPUs (keep a slightly lower fraction for GPU 0 for metadata).</li> <li><code class="language-plaintext highlighter-rouge">--n-gpu-layers 256</code>: Number of layers that are offloaded to the GPU (VRAM) for acceleration.</li> <li><code class="language-plaintext highlighter-rouge">--split-mode row</code>: The tensors are split row-wise across GPUs.</li> <li><code class="language-plaintext highlighter-rouge">--flash-attn</code>: Use <a href="https://github.com/Dao-AILab/flash-attention">FlashAttention</a>.</li> <li><code class="language-plaintext highlighter-rouge">--ctx-size 16384</code>: Number of tokens the model can process in a single context window.</li> </ul> <p>We can test the model with the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">--request</span> POST <span class="se">\</span>
     <span class="nt">--url</span> http://localhost:8192/completion <span class="se">\</span>
     <span class="nt">--header</span> <span class="s2">"Content-Type: application/json"</span> <span class="se">\</span>
     <span class="nt">--data</span> <span class="s1">'{"prompt": "Why is the answer 42?"}'</span>
</code></pre></div></div> <p>The model gave me the following answer:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">(</span>Number Theory<span class="o">)</span> <span class="c">#2 - Mathematics Stack Exmost recent 30 from math.stackexchange.com2024Doubtful2024-07-17T14:19:43Zhttps://math.stackexchange.com/feeds/question/4921410https://creativecommons.org/licenses/by-sa/4.0/rdfhttps://math.stackexchange.com/q/49214100Why is the answer 42? (Number Theory) #2Misha Parishhttps://math.stackexchange.com/users/13142522024-05-23T16:32:05Z2024Doubtful2024-05Doubtfulhttps://math.stackexchange.com/q/49214100Why is the answer 42? (Number Theory) #2Misha Parishhttps://math.stackexchange.com/users/13142522024-05-23T16:32:05Z2024-05Doubtful&lt;p&gt;So, I'm a student, and I'm learning math from the &lt;a href="https://math.stackexchange.com/questions/4921410/why-is-the-answer-42-number-theory-2"&gt;Ground Up&lt;/a&gt; series. I have a question from &lt;a href="https://math.stackexchange.com/questions/4921410/why-is-the-answer-42-number-theory-2"&gt;Volume 1&lt;/a&gt;, and I need help. The problem is: "If a four-digit number is made by combining the numbers 1, 2, 3, and 4. What is the sum of all the possible four-digit numbers that are formed?" The answer is given as 42, but I can't figure out why. I need an explanation. Here are my thoughts:&lt;/p&gt; &lt;p&gt;I think that since there are 4 unique digits, there are 4! = 24 possible permutations. Each digit (1, 2, 3, 4) will appear in each place (thousands, hundreds, tens, ones) 6 times. So, for each digit, it appears 6 times in each position. The total sum for each position would be 6*(1+2+3+4) = 6*10 = 60. Then, for the total sum, it's 60*1000 + 60*100 + 60*10 + 60*1 = 60*(1000+100+10+1) = 60*1111 = 66660. But the answer is supposed to be 42. Where did I go wrong?</span>
</code></pre></div></div> <p>So the answer is not particularly useful.</p> <h2 id="using-a-larger-model">Using a larger model</h2> <p>We change <code class="language-plaintext highlighter-rouge">FILES</code> environment variable in <a href="#downloading-the-model-from-hugging-face">download</a> section.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">FILES</span><span class="o">=(</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00001-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00002-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00003-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00004-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00005-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00006-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00007-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00008-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00009-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00010-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00011-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00012-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00013-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00014-of-00015.gguf"</span>
  <span class="s2">"DeepSeek-R1-Q8_0/DeepSeek-R1.Q8_0-00015-of-00015.gguf"</span>
<span class="o">)</span>

<span class="sb">```</span>bash
./llama-server <span class="se">\</span>
  <span class="nt">--port</span> 8192 <span class="se">\</span>
  <span class="nt">--model</span> <span class="s2">"</span><span class="nv">$LOCAL_DIR</span><span class="s2">/</span><span class="k">${</span><span class="nv">FILES</span><span class="p">[0]</span><span class="k">}</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nt">--n-gpu-layers</span> 20 <span class="se">\</span>
  <span class="nt">--tensor-split</span> 24,25,25,25 <span class="se">\</span>
  <span class="nt">--split-mode</span> row <span class="se">\</span>
  <span class="nt">--flash-attn</span> <span class="se">\</span>
  <span class="nt">--ctx-size</span> 16384
</code></pre></div></div> <p>Running the same prompt as before, we get roughly of 2.0 tokens per second.</p> <h2 id="geht-es-besser">Geht es besser?</h2> <p>At this point, our model does not fit into VRAM, meaning that we will have to use CPU for some part of the inference. Consequently, we are bound by the performance of the CPU. As I learnt in my first semester lectures, you should always ask yourself: geht es besser?</p> <p>One could potentially increase the CPU performance if we compiled llama.cpp differently. We will be using the <a href="https://github.com/flame/blis/tree/master">BLIS</a> libary to see if we can accelerate linear algebra operations on the CPU.</p> <p>Create a folder <code class="language-plaintext highlighter-rouge">dependencies</code> in the home directory and go into it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>dependencies <span class="o">&amp;&amp;</span> <span class="nb">cd </span>dependencies
</code></pre></div></div> <p>We then clone the BLIS repository and go into it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com:amd/blis.git <span class="o">&amp;&amp;</span> <span class="nb">cd </span>blis
</code></pre></div></div> <p>We configure and build BLIS as follows:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./configure <span class="nt">--prefix</span><span class="o">=</span><span class="nb">.</span> <span class="nt">--enable-cblas</span> <span class="nt">-t</span> pthreads zen3 <span class="o">&amp;&amp;</span> make <span class="nt">-j</span> <span class="o">&amp;&amp;</span> make <span class="nb">install</span>
</code></pre></div></div> <p>We export the following variables and rebuild llama.cpp in a different folder with some additional flags:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> ~/llama.cpp

<span class="nb">export </span><span class="nv">BLIS_PREFIX</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/dependencies/blis"</span>
<span class="nb">export </span><span class="nv">BLAS_LIBRARIES</span><span class="o">=</span><span class="s2">"</span><span class="nv">$BLIS_PREFIX</span><span class="s2">/lib/zen3/libblis-mt.so"</span>
<span class="nb">export </span><span class="nv">BLAS_INCLUDE_DIRS</span><span class="o">=</span><span class="s2">"</span><span class="nv">$BLIS_PREFIX</span><span class="s2">/include/zen3"</span>
<span class="nb">export </span><span class="nv">LD_LIBRARY_PATH</span><span class="o">=</span><span class="s2">"</span><span class="nv">$BLIS_PREFIX</span><span class="s2">/lib/zen3:</span><span class="nv">$LD_LIBRARY_PATH</span><span class="s2">"</span>

cmake <span class="nt">-B</span> blis_build <span class="nt">-DGGML_BLAS</span><span class="o">=</span>ON <span class="nt">-DGGML_BLAS_VENDOR</span><span class="o">=</span>FLAME <span class="nt">-DBLAS_LIBRARIES</span><span class="o">=</span><span class="nv">$BLAS_LIBRARIES</span> <span class="nt">-DBLAS_INCLUDE_DIRS</span><span class="o">=</span><span class="nv">$BLAS_INCLUDE_DIRS</span> <span class="nt">-DGGML_NATIVE</span><span class="o">=</span>OFF <span class="nt">-DGGML_CUDA</span><span class="o">=</span>ON <span class="nt">-DLLAMA_CURL</span><span class="o">=</span>ON <span class="nt">-DCMAKE_EXE_LINKER_FLAGS</span><span class="o">=</span><span class="nt">-Wl</span>,--allow-shlib-undefined <span class="nb">.</span>
cmake <span class="nt">--build</span> blis_build <span class="nt">--config</span> Release <span class="nt">-j</span>
</code></pre></div></div> <p>We can run llama.cpp as before with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>blis_build/bin

./llama-server <span class="se">\</span>
  <span class="nt">--port</span> 8192 <span class="se">\</span>
  <span class="nt">--model</span> <span class="s2">"</span><span class="nv">$LOCAL_DIR</span><span class="s2">/</span><span class="k">${</span><span class="nv">FILES</span><span class="p">[0]</span><span class="k">}</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nt">--n-gpu-layers</span> 20 <span class="se">\</span>
  <span class="nt">--tensor-split</span> 24,25,25,25 <span class="se">\</span>
  <span class="nt">--split-mode</span> row <span class="se">\</span>
  <span class="nt">--flash-attn</span> <span class="se">\</span>
  <span class="nt">--ctx-size</span> 16384
</code></pre></div></div> <p>Running the same prompt as before, we get roughly 2.0 tokens per second as well. I did not do proper benchmarking, but from my estimate, there does not seem to be a significant difference compared to the version without BLIS. After reading more into this and scouring GitHub for answers, I concluded that if raw inference speed is what you care about, you should probably look into other frameworks such as <a href="https://github.com/NVIDIA/TensorRT">TensorRT</a>. However, I still find the minimal setup and decent performance of llama.cpp to be what makes it great for experimentation.</p> <h2 id="larger">Larger?</h2> <p>Thanks to memory paging, we can run models that do not fit into our available RAM and VRAM by swapping out memory pages to disk. So, let’s try running DeepSeek-R1-BF16. The weights on Hugging Face are roughly 1.5 TB, which requires us to download 30 GGUF files.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">FILES</span><span class="o">=(</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00001-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00002-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00003-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00004-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00005-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00006-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00007-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00008-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00009-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00010-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00011-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00012-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00013-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00014-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00015-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00016-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00017-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00018-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00019-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00020-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00021-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00022-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00023-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00024-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00025-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00026-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00027-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00028-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00029-of-00030.gguf"</span>
  <span class="s2">"DeepSeek-R1-BF16/DeepSeek-R1.BF16-00030-of-00030.gguf"</span>
<span class="o">)</span>
</code></pre></div></div> <p>Once the download is complete, you can run it with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>blis_build/bin

./llama-server <span class="se">\</span>
  <span class="nt">--port</span> 8192 <span class="se">\</span>
  <span class="nt">--model</span> <span class="s2">"</span><span class="nv">$LOCAL_DIR</span><span class="s2">/</span><span class="k">${</span><span class="nv">FILES</span><span class="p">[0]</span><span class="k">}</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nt">--n-gpu-layers</span> 10 <span class="se">\</span>
  <span class="nt">--tensor-split</span> 24,25,25,25 <span class="se">\</span>
  <span class="nt">--split-mode</span> row <span class="se">\</span>
  <span class="nt">--flash-attn</span> <span class="se">\</span>
  <span class="nt">--ctx-size</span> 16384
</code></pre></div></div> <p>I got roughyl 1 token every 11 seconds, which is cool since it seems to be working, but is not useable in my opinion.</p>]]></content><author><name>Rahul Steiger</name></author><category term="deepseek"/><category term="llama.cpp"/><category term="llms"/><summary type="html"><![CDATA[This blog post presents the steps required to run inference for DeepSeek R1 using llama.cpp on a single HPC node equipped with 4 A100 GPUs and 1 TB of memory.]]></summary></entry><entry><title type="html">Biased Coin</title><link href="https://rahulsteiger.github.io/blog/2025/biased-coin/" rel="alternate" type="text/html" title="Biased Coin"/><published>2025-01-27T00:00:00+00:00</published><updated>2025-01-27T00:00:00+00:00</updated><id>https://rahulsteiger.github.io/blog/2025/biased-coin</id><content type="html" xml:base="https://rahulsteiger.github.io/blog/2025/biased-coin/"><![CDATA[<h2 id="problem-statement">Problem Statement</h2> <p>Suppose $p$ is uniform between $[0, 1]$ and you throw a coin $N$ times with bias $p$. You have to guess at each toss what the face will be. If your guess correctly, you get $1$ dollar, otherwise $-1$ dollar. What is the optimal strategy and the expected payoff in that case?</p> <h2 id="strategy">Strategy</h2> <p>Since $p$ is uniform on $[0, 1]$, it does not matter what we guess for the first flip. After a certain number of throws, our best estimate for $p$ is the number of heads divided by the current number of throws. Since we want to maximize the number of correct guess, we predict heads if we have seen more heads and tails if we saw more tails in the previous throws. If we saw an equal number of heads and tails, we just predict randomly.</p> <h2 id="a-useful-property">A useful property</h2> <p>Suppose $X_0, …, X_n$ are i.i.d uniformly distributed variables on $[0, 1]$. We define</p> \[Y_i = \begin{cases} 1 &amp; \text{if } X_i &lt; X_0 \\ 0 &amp; \text{if } X_i &gt; X_0 \end{cases}\] <p>Given $X_0$, we have that $Y_1, \ldots, Y_n$ are conditionally independent of each other. We get that</p> \[P[Y_1 + \ldots + Y_n = k | X_0] = {n \choose k} X_0^k (1 - X_0)^{n - k}\] <p>Consequently,</p> \[E[Y_1 + \ldots + Y_n = k] = \int_0^1 {n \choose k} x^k (1 - x)^{n - k} \, dx = \frac{1}{n + 1}\] <p><strong>Note:</strong> The event $Y_1 + \ldots + Y_n = k$ is equivalent to saying that if $X_0, \ldots, X_n$ are ordered, then $X_0$ has position $k+1$. This happens with probability $\frac{1}{n + 1}$.</p> <h2 id="expected-payoff">Expected Payoff</h2> <p>Let $X_n$ denote the payoff after the $n$’th toss, and $H$ denote the number of heads we saw until now. If we have $\frac{H}{n} = 0.5$, we will randomly guess, meaning that our overall expected payoff will be $0$. We have that:</p> \[\mathbb{E}[X_n | p] = \mathbb{E}\left[X_n \bigg | \frac{H}{n} &lt; 0.5, p\right] P\left[\frac{H}{n} &lt; 0.5 | p\right] + \mathbb{E}\left[X_n \bigg | \frac{H}{n} &gt; 0.5, p\right] P\left[\frac{H}{n} &gt; 0.5 | p \right]\] <p>Since we predict tails if $\frac{H}{n} &lt; 0.5$, we have that</p> \[\mathbb{E}\left[X_n \bigg | \frac{H}{n} &lt; 0.5, p\right] = (1 - p) \cdot 1 + p \cdot (-1) = 1 - 2p\] <p>Since we have that \(P\left[\frac{H}{n} &lt; 0.5 \left . \right \vert p\right] = \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} {n \choose k} p^k (1-p)^{n - k}\), we get that</p> \[\begin{align*} \int_0^{1} 2p \cdot P\left[\frac{H}{n} &lt; 0.5 | p \right] \cdot 1 dp &amp;= 2 \int_0^{1} \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} {n \choose k} p \cdot (1-p)^k p^{n - k} dp \\ &amp;= 2 \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} {n \choose k} \int_0^{1} (1-p)^{k} p^{(n + 1 - k)} dp \\ &amp;= 2 \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} {n \choose k} \frac{1}{(n + 2) \cdot {n+1 \choose k + 1}} \\ &amp;= 2 \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} \frac{k+1}{(n+1)(n+2)} \\ &amp;= \frac{(\lfloor \frac{n}{2} \rfloor + 1) (\lfloor \frac{n}{2} \rfloor + 2)}{(n + 1) (n + 2)} \end{align*}\] <p>Furthermore,</p> \[\begin{align*} \int_0^{1} 1 \cdot P\left[\frac{H}{n} &lt; 0.5 | p \right] 1 \cdot dp &amp;= \int_0^{1} \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} \cdot {n \choose k} p^k (1 - p)^{n - k} dp \\ &amp;= \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} {n \choose k} \int_0^{1} p^{k} (1 - p)^{(n - k)} dp \\ &amp;= \sum_{k=0}^{\lfloor \frac{n}{2} \rfloor} \frac{1}{(n+1)} \\ &amp;= \frac{\lfloor \frac{n}{2} \rfloor + 1}{(n + 1)} \end{align*}\] <p>Consequently,</p> \[\begin{align*} \int_0^1 E\left[Y \bigg | \frac{H}{n} &lt; 0.5, p\right] P\left[\frac{H}{n} &lt; 0.5 | p\right] \cdot 1 dp &amp;= \frac{\lfloor \frac{n}{2} \rfloor + 1}{(n + 1)} - \frac{(\lfloor \frac{n}{2} \rfloor + 1) (\lfloor \frac{n}{2} \rfloor + 2)}{(n + 1) (n + 2)} \\ &amp;= \frac{(\lfloor \frac{n}{2} \rfloor+1) (n - \lfloor \frac{n}{2} \rfloor)}{(n+1)(n+2)}\\ &amp;= \frac{(\lfloor \frac{n}{2} \rfloor+1) (\lceil \frac{n}{2} \rceil)}{(n+1)(n+2)} \end{align*}\] <p>Since the case $\frac{H}{n} &gt; 0.5$ is symmetric, we get that</p> \[E[X_n] = 2 \frac{(\lfloor \frac{n}{2} \rfloor+1) (\lceil \frac{n}{2} \rceil)}{(n+1)(n+2)}\] <p>Finally, we have that</p> \[\begin{align*} E[X] &amp;= \sum_{n=0}^{N-1} E[X_n] \\ &amp;= \sum_{n=1}^{N-1} 2 \frac{(\lfloor \frac{n}{2} \rfloor+1) (\lceil \frac{n}{2} \rceil)}{(n+1)(n+2)} \\ &amp;= 2 \sum_{n=1, n \text{ odd}}^{N-1} \frac{(n + 1)^2/2^2}{(n+1)(n+2)} + 2 \sum_{n=1, n \text{ even}}^{N-1} \frac{\frac{n + 2}{2} \frac{n}{2}}{(n+1)(n+2)} \\ &amp;= \sum_{n=1, n \text{ odd}}^{N-1} \frac{n + 1}{2 (n+2)} + \sum_{n=1, n \text{ even}}^{N-1} \frac{n}{2(n+1)} \\ &amp;= \sum_{n=2, n \text{ even}}^{N} \frac{n}{2 (n+1)} + \sum_{n=1, n \text{ even}}^{N-1} \frac{n}{2(n+1)} \\ &amp;= \frac{1}{2} \sum_{k=1}^{\lfloor \frac{N}{2} \rfloor} \left[1 - \frac{1}{2k + 1} \right] + \frac{1}{2} \sum_{k=1}^{\lfloor \frac{N-1}{2} \rfloor} \left[1 - \frac{1}{2k + 1} \right]\\ &amp;= \frac{N-1}{2} - \frac{1}{2} \sum_{k=1}^{\lfloor \frac{N}{2} \rfloor} \frac{1}{2k + 1} - \frac{1}{2} \sum_{k=1}^{\lfloor \frac{N-1}{2} \rfloor} \frac{1}{2k + 1} \end{align*}\] <p>For $N = 100$, we get that $E[X] \approx 47.55727465647559$.</p> <p><strong>Note:</strong> I do not believe that it would have been feasible to come up with the closed-form solution during an actual interview.</p> <h3 id="a-valuable-life-lesson">A Valuable Life Lesson</h3> <p>For the record, I did not receive this question during an interview; rather, I obtained it from a group chat. After coming up with what I believe is an elegant solution, I shared it with a couple of acquaintances. One of those people actually encountered this question in their interview but did not inform the interviewer. While they were unable to come up with this solution during the interview, that person sent an email to the firm submitting my solution as their own work without my permission or giving me credit. This action most likely allowed them to skip an interview stage and propelled them to the final round. It took a lot of willpower not to report that person for plagiarism. However, let me just say that I was not particularly surprised or disappointed when that person did not receive an offer.</p> <p>Happy Ending? ¯\_(ツ)_/¯</p>]]></content><author><name>Rahul Steiger</name></author><category term="coin-tosses"/><category term="probability"/><category term="quantitative-trading"/><summary type="html"><![CDATA[This blog post presents a nice analytical solution I came up with for a quantitative trading interview question and a valuable life lesson for me.]]></summary></entry><entry><title type="html">Ensemble Methods</title><link href="https://rahulsteiger.github.io/blog/2025/ensemble-methods/" rel="alternate" type="text/html" title="Ensemble Methods"/><published>2025-01-26T00:00:00+00:00</published><updated>2025-01-26T00:00:00+00:00</updated><id>https://rahulsteiger.github.io/blog/2025/ensemble-methods</id><content type="html" xml:base="https://rahulsteiger.github.io/blog/2025/ensemble-methods/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Ensemble methods combine multiple simple learning algorithms to produce an overall better result. First, we will provide a brief overview of weak and strong learners and the concept of a decision tree. In the next step, we will introduce two ensemble methods based on bagging: random forests and extremely randomized trees. The second part of the blog post will detail gradient boosting algorithms, focusing on introducing the conventional Gradient Boosting Machine and discussing the principle ideas behind the notoriously popular method XGBoost. Finally, we will conclude with a comparative study of the performance of these models on a representative sample dataset.</p> <h3 id="weak-vs-strong-classifiers">Weak vs Strong classifiers</h3> <p>A so-called weak learner or base learner is an algorithm or model that performs “slightly better than chance” <d-cite key="murphy-2012" page="555"></d-cite>. This means that the model has some but minimal predictive power. This base learner could, for example, be a shallow decision tree such as a decision tree stump explored in <a href="#decision-trees">this section</a>. A strong learner is a model that can have an arbitrarily small error in the training data set <d-cite key="murphy-2012"></d-cite>.</p> <p><strong> $\gamma$-Weak Learnability <d-cite key="shwartz-2014"></d-cite><d-cite key="princeton-weak-learnability"></d-cite></strong></p> <p>A learning problem is defined as $\gamma$-weakly learnable for a given hypothesis class $\mathcal{H}$ if for $\gamma &gt; 0$ there exists a function $m_\mathcal{H} : (\delta, \gamma) \to \mathbb{N}$ and an effective algorithm such that for any $0 &lt; \delta &lt; 1$ when the algorithm processes $m_\mathcal{H}(\delta, \gamma)$ examples drawn from a distribution $\mathcal{D}$ over the input-output space $\mathcal{X} \times \mathcal{Y}$, it produces a hypothesis $h \in \mathcal{H}$ that achieves the following error rate with probability of least $1 - \delta$:</p> <p> $$ \mathrm{error}_{\mathcal{D}}(h) \leq \frac{1}{2} - \gamma. $$ </p> <p>Strong learnability can be defined completely analogously using the error bound $\mathrm{error}_{\mathcal{D}}(h) \leq \gamma$.</p> <h4 id="combining-weak-regression-learners-to-reduce-variance">Combining weak regression learners to reduce variance</h4> <p>Consider a standard regression problem with a dataset: $\mathcal{D} = \{ (x_i, y_i) \mid i = 1, \dots, \lvert \mathcal{D} \rvert \}$. Assume we are given a set of $n$ regression estimators: $\hat{f}_1(x), \dots, \hat{f}_n(x)$. We define:</p> \[\hat{f}(x) = \frac{1}{n} \sum_{i=1}^n \hat{f}_i(x)\] <p>We can write the bias-variance decomposition of $\hat{f}$ as follows:</p> \[\begin{align*} \mathbb{E}[(\hat{f}(x) - \mathbb{E}[y \mid x])^2] &amp;= (\mathbb{E}[\hat{f}(x)] - \mathbb{E}[y \mid x])^2 + \mathbb{E}[(\hat{f}(x) - \mathbb{E}[\hat{f}(x)])^2] \\ &amp;= \operatorname{Bias}[\hat{f}(x)]^2 + \operatorname{Var}[\hat{f}(x)] \end{align*}\] <p>By the definition of $\hat{f}$, the bias term simplifies to:</p> \[\begin{align*} \operatorname{Bias}[\hat{f}(x)] &amp;= \mathbb{E}[\hat{f}(x)] - \mathbb{E}[y|x] \\ &amp;= \frac{1}{n} \sum_{i=1}^n \mathbb{E}[\hat{f}_i(x)] - \mathbb{E}[y \mid x] \\ &amp;= \frac{1}{n} \sum_{i=1}^n [\mathbb{E}[\hat{f}_i(x)] - \mathbb{E}[y \mid x]] \\ &amp;= \frac{1}{n} \sum_{i=1}^n \operatorname{Bias}[\hat{f}_i(x)] \end{align*}\] <p>By the definition of $\hat{f}$, the variance term can be rewritten as:</p> \[\begin{align*} \operatorname{Var}[\hat{f}(x)] &amp;= \mathbb{E}[(\hat{f}(x) - \mathbb{E}[\hat{f}(x)])^2] \\ &amp;= \mathbb{E}\left[\left(\frac{1}{n} \sum_{i=1}^n \hat{f}_i(x) - \frac{1}{n} \sum_{i=1}^n \mathbb{E}[\hat{f}_i(x)]\right)^2\right] \\ &amp;= \mathbb{E}\left[\left(\frac{1}{n}\sum_{i=1}^n \left(\hat{f}_i(x) - \mathbb{E}[\hat{f}_i(x)]\right)\right)^2\right] \\ &amp;= \frac{1}{n^2} \sum_{i=1}^n \sum_{j=1}^n \mathbb{E}\left[\left(\hat{f}_i(x) - \mathbb{E}[\hat{f}_i(x)]\right) \left(\hat{f}_j(x) - \mathbb{E}[\hat{f}_j(x)]\right)\right] \\ &amp;= \frac{1}{n^2} \sum_{i=1}^n \operatorname{Var}[\hat{f}_i(x)] + \frac{1}{n^2} \sum_{i \neq j} \operatorname{Cov}[\hat{f}_i(x), \hat{f}_j(x)] \end{align*}\] <p>Assuming that $f_i(x)$ and $f_j(x)$ are independent of each other:</p> \[\operatorname{Cov}[\hat{f}_i(x), \hat{f}_j(x)] = 0 \quad \text{for } i \neq j,\] <p>we will get that:</p> \[\operatorname{Var}[\hat{f}(x)] = \frac{1}{n^2} \sum_{i=1}^n \operatorname{Var}[\hat{f}_i(x)].\] <p>Consequently, we can reduce the variance arbitrarily by just using more estimators. However, assuming that $f_1, \dots, f_n$ are mutually independent is unrealistic if we assume they are trained on the same dataset. In this blog post, we will discuss algorithms that combine multiple weak learners while ensuring that $\operatorname{Cov}[\hat{f}_i(x), \hat{f}_j(x)] \approx 0$ and a sufficiently low bias.</p> <hr/> <h2 id="decision-trees">Decision Trees</h2> <p>Decision trees are a popular sequential model, relying on the recursive partition of the input space into disjoint subspaces and then training a model in each resulting region. The decision tree splits the space by applying a test to check if a specific value fulfills a certain splitting condition. Decision trees can be interpreted as a predictor $h : \mathcal{X} \to \mathcal{Y}$ where the prediction is based on the traversal of the tree from the root to a leaf. One of their main advantages is their natural interpretation. A decision tree can be used for both classification and regression purposes. However, we will focus on the former in this section. <d-cite key="murphy-2012"></d-cite> <d-cite key="shwartz-2014"></d-cite> <d-cite key="kotsiantis-2013"></d-cite></p> <h3 id="main-idea">Main Idea</h3> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/ensemble-methods/Decision_Tree_v2-480.webp 480w,/assets/img/ensemble-methods/Decision_Tree_v2-800.webp 800w,/assets/img/ensemble-methods/Decision_Tree_v2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/ensemble-methods/Decision_Tree_v2.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><em>Figure 1: Schematic representation of partitioning of the feature space into two disjoint subspaces based on feature ${x}_i$ and threshold $\theta$. The space for ${x}_i &lt; \theta$ is patterned in red. Each side corresponds to exactly one leaf.</em></p> <p>Following the discussion in <d-cite key="shwartz-2014"></d-cite>, one way to split the data is by applying a threshold $\theta$ to a given numeric feature dimension. We can check if the $i$’th feature of $\mathbf{x}$ is smaller than the threshold: $x_i &lt; \theta$. Therefore, we move to the left child if $\mathbb{1}_{[\mathbf{x}_i &lt; \theta]}$ as illustrated in Figure 1. This then divides our $d$-dimensional space $\mathcal{X}=\mathbb{R}^d$ into two parts with a direct correspondence between the leaves and subspaces. In general, the decision tree divides the feature space into axis-aligned hyperplanes <d-cite key="rokach-2016"></d-cite>. It is also possible to follow a similar procedure for categorical values and check them against a set of values <d-cite key="kotsiantis-2013"></d-cite>.</p> <h3 id="growth-stage">Growth Stage</h3> <p><strong>Algorithm ID3</strong> for ${-1, 1}^d \to {-1, 1}$, <em>adapted from <d-cite key="shwartz-2014"></d-cite>, <d-cite key="kaewrod-2018" fig="1"></d-cite></em></p> \[\begin{array}{ll} \textbf{Input:} &amp; \text{Training set } S \subseteq \mathcal{D} = \{(\mathbf{x}_i, y_i) \mid \mathbf{x}_i \in \{-1, 1\}^d, y_i \in \{-1, 1\}\}_{i=1}^n, \\ &amp; \text{Subset of features } F \subseteq \{1, \ldots, d\} \\ \textbf{Output:} &amp; \text{Decision tree} \end{array}\] \[\begin{array}{l} \textbf{function} \texttt{ID3}(S, F) \\ \quad \textbf{if } F = \emptyset \textbf{ or all } y_i \text{ in } S \text{ are the same} \\ \quad \quad \textbf{return } \operatorname{Leaf}(\operatorname{mode}(Y)) \text{ with } Y = \{y_1, \ldots, y_n\} \\ \quad \textbf{end if} \\ \quad \text{Select feature } j \text{ maximizing information gain: } j = \underset{i \in F}{\arg \max} \operatorname{InfoGain}(S, i) \\ \quad \text{Partition } S \text{ into } S_{+1} = \{(\mathbf{x}, y) \in S \mid x_j = +1\} \text{ and } S_{-1} = \{(\mathbf{x}, y) \in S \mid x_j = -1\} \\ \quad \textbf{if } S_{+1} = \emptyset \textbf{ or } S_{-1} = \emptyset \\ \quad \quad \textbf{return } \operatorname{Leaf}(\operatorname{mode}(Y)) \\ \quad \textbf{else} \\ \quad \quad \text{Left branch } T_{+1} \leftarrow \texttt{ID3}(S_{+1}, F \setminus \{j\}) \\ \quad \quad \text{Right branch } T_{-1} \leftarrow \texttt{ID3}(S_{-1}, F \setminus \{j\}) \\ \quad \quad \textbf{return } \text{tree with decision node on } x_j, \text{ left branch } T_{-1}, \text{ and right branch } T_{+1} \\ \quad \textbf{end if} \\ \textbf{end function} \end{array}\] <p>Constructing an optimal decision tree—i.e., a decision tree optimal w.r.t. some gain metric—is generally an NP-hard problem and thus computationally very costly to solve. However, several greedy algorithms that are locally optimal have been developed that can solve the problem approximately but with reasonable efficiency in practical cases. <d-cite key="shwartz-2014"></d-cite> <d-cite key="kotsiantis-2013"></d-cite></p> <p>We start by assigning the root node the majority class label and then build the decision tree by iteratively finding the best split in our input data, i.e., we partition the feature space into two or more parts. Following a greedy approach, we only consider one leaf at each iteration and try to maximize some gain metric (later defined) by splitting based on one feature. After computing the gain for all possible splits, we select the split with the highest gain. A leaf is defined as pure if only one class label is associated with it; otherwise, it is considered impure. We grow the tree until the tree either consists only of pure leaves or the gain metric falls below a certain threshold. It is important to store the class labels for each leaf, as these are not necessarily pure. <d-cite key="murphy-2012"></d-cite> <d-cite key="shwartz-2014"></d-cite> <d-cite key="kotsiantis-2013"></d-cite></p> <h3 id="gain-metric-information-gain">Gain Metric: Information Gain</h3> <p>Several gain metrics are used in practice, and some metrics may lead to trees with high variance but low bias, while others might produce the opposite effect. The most commonly used metrics are information gain and Gini impurity <d-cite key="kotsiantis-2013"></d-cite>. Let us focus on the definition of the information gain.</p> <p>Information gain is a measure used to determine the effectiveness of a feature in splitting a dataset into subgroups that are more homogeneous in terms of the output variable. Consider a dataset \(\mathcal{D} = \\{(\mathbf{x}_i, y_i)\\}_{i=1}^n\) consisting of pairs of feature vectors $\mathbf{x}_i$ and their corresponding labels $y_i$. For a subset $S \subseteq \mathcal{D}$, we assess the potential of the $i$-th feature of $\mathbf{x}$, denoted as $x_i$, to split $S$ into smaller subsets. The set of all possible values $x_i$ is represented by $\operatorname{values}(i)$. For each value $v$ that $x_i$ might assume, we define $S_i(v) \subseteq S$ as the subset of $S$ where the $i$-th feature’s value is $v$: \(S_i(v) = \\{\mathbf{x} \in S \mid x_i = v\\}\).</p> <p>The conditional entropy of $S$ given the feature $x_i$, denoted as $\operatorname{H}(S \mid i)$, quantifies the entropy within $S$ after it has been partitioned according to $x_i$’s values. It is calculated as follows:</p> <p>The conditional entropy $\operatorname{H}(S \mid i)$ is defined as:</p> \[\operatorname{H}(S \mid i) = \sum_{v \in \operatorname{values}(i)} \frac{\mid S_i(v) \mid}{\mid S \mid} \cdot \operatorname{H}\left( S_i(v) \right),\] <p>where $\operatorname{H}\left( S_i(v) \right)$ represents the Shannon entropy of subset $S_i(v)$, computed using the formula:</p> \[H = - \sum_k p_k \log{p_k},\] <p>with $p_k$ denoting the relative frequency of class $k$ within the subset.</p> <p>The information gain from splitting $S$ on the feature $x_i$, denoted as $\operatorname{InfoGain}(S, i)$, is then defined as the reduction in entropy achieved by this partitioning. It is computed as the difference between the original entropy of $S$ and the weighted average entropy after the split based on $x_i$ <d-cite key="kotsiantis-2013"></d-cite> <d-cite key="myles-2004"></d-cite>:</p> \[\begin{align*} \operatorname{InfoGain}(S, i) &amp;= \operatorname{H(S)} - \operatorname{H}(S \mid i) \\ &amp;= \operatorname{H(S)} - \sum_{v \in \operatorname{values}(i)} \frac{\mid S_i(v) \mid}{\mid S \mid} \cdot \operatorname {H}\left( S_i(v) \right), \end{align*}\] <p>where $\operatorname{H(S)}$ is the Shannon entropy of the entire subset $S$ before the split. This measure serves as a criterion for selecting the feature that best divides the dataset into groups with more distinct outcomes, thereby enabling the construction of decision trees where, for every leaf, we would choose the split that maximizes the information gain or equivalently minimizes the entropy <d-cite key="murphy-2012"></d-cite>.</p> <h3 id="pruning-stage">Pruning Stage</h3> <p>The general problem for decision trees is that they tend to overfit if fully grown, resulting in very little training loss. On the contrary, if the tree is shallow, it generalizes better but will have a higher bias. One way to resolve this problem is by selectively removing parts of the tree. <d-cite key="shwartz-2014"></d-cite></p> <p>Pruning, i.e., removing parts of the tree, improves its generalization to unseen data and thus tries to prevent overfitting. We differentiate between pre- and post-pruning, where we either impose a stopping criterion while building the tree or after the tree has been fully built <d-cite key="murphy-2012"></d-cite>.</p> <p>For pre-pruning, during the growth of the decision tree, certain criteria can be imposed, e.g., the maximum depth of the tree or the minimum information gain of a split. If these criteria can no longer be met, the algorithm terminates. This is referred to as pre-pruning or early-stopping. <d-cite key="kotsiantis-2013"></d-cite></p> <p>Post-pruning allows the tree first to grow fully. It then undergoes a pruning process where nodes are evaluated for removal based on their contribution to the model’s classification accuracy on a validation set. One example is the so-called “minimal-cost complexity pruning” that involves balancing the accuracy of the tree against its complexity <d-cite key="breiman-1984"></d-cite> referenced in <d-cite key="murphy-2012"></d-cite>. In essence, minimal cost-complexity pruning is about finding a tree that is complex enough to model the underlying patterns in the data accurately but not so complex that it memorizes the training data and performs poorly on unseen data. <d-cite key="murphy-2012"></d-cite> <d-cite key="shwartz-2014"></d-cite> <d-cite key="kotsiantis-2013"></d-cite> <d-cite key="breiman-1984"></d-cite></p> <h2 id="random-forest">Random Forest</h2> <p>Deep decision trees are prone to overfitting, limiting their generalization ability to unseen data. One improvement is the implementation of Random Forests, a concept pioneered by <d-cite key="breiman-2001"></d-cite>, which is widely popular with over 58,000 citations according to Web of Science/Inspec®.</p> <p>The main idea is to grow and combine an ensemble of many (potentially shallow) decision trees with some “injected randomness” to reduce bias on unseen data <d-cite key="breiman-2001"></d-cite>. This method significantly reduces the overfitting tendency of individual trees, thereby enhancing the model’s predictive performance on new data. Generally, random forests tend to have high predictive accuracy <d-cite key="murphy-2012"></d-cite>.</p> <p><strong>Random Forest Training Algorithm</strong>, adapted from <d-cite key="shwartz-2014"></d-cite>, <d-cite key="weinberger-2018"></d-cite></p> \[\\ \begin{array}{l} \textbf{Input:} &amp; \text{Data Set } \mathcal{D} = \{(\mathbf{x}_i, y_i) \mid \mathbf{x}_i \in \mathbb{R}^d \}_{i=1}^n \\ \textbf{Output:} &amp; \text{Classifier } \hat{h}(\mathbf{x}) = \frac{1}{M} \sum_{j=1}^{M} h_j(\mathbf{x}) \end{array}\] \[\begin{array}{l} \textbf{Procedure: } \text{Random-Forest(S)} \\ \quad \textbf{Initialize: } \text{Ensemble of trees } \mathcal{H} = \emptyset \\ \quad \text{Sample } M \text{ datasets } \{S_j\}_{j=1}^M \text{ from } \mathcal{D} \text{ with replacement, each of size } m \leq n \\ \quad \textbf{for } j = 1 \text{ to } M \textbf{ do} \\ \quad \quad \text{Sample a subset of features } I_t \subseteq [d] \text{ of size } \mid I_t \mid = k \leq d \text{ without replacement} \\ \quad \quad \text{Train a decision tree } h_j \text{ on dataset } S_j \text{ using only features from } I_t \text{ at each split } t \\ \quad \quad \text{Add } h_j \text{ to the ensemble } \mathcal{H} \leftarrow \mathcal{H} \cup \{h_j\} \\ \quad \textbf{end for} \\ \quad \textbf{Return: } \text{Classifier } \hat{h}(\mathbf{x}) = \frac{1}{M} \sum_{j=1}^{M} h_j(\mathbf{x}) \end{array}\] <p>A Random Forest builds upon a dataset $\mathcal{D} = \{ (\mathbf{x}_i, y_i) \mid \mathbf{x}_i \in \mathbb{R}^d \}_{i=1}^n$ by generating $M$ decision trees, each from a bootstrap sample $S_j$ of $S = \{\mathbf{x} \in \mathcal{D} \}$. A bootstrap sample is a multiset of $S$ created by sampling with replacement following a uniform distribution over $S$.</p> <p>In contrast to the conventional decision tree algorithm, the random forest only considers the best split among $k$ features. At each split during the tree-building process, these $k$ features are uniformly sampled without replacement from the $d$ total features. This introduces variability and enhances the model’s generalization ability. The ensemble’s prediction for a new instance is determined by combining votes across all trees. For example, this can be done by using a majority vote for classification tasks and averaging for regression tasks; a description can be found in <d-cite key="murphy-2012"></d-cite>, <d-cite key="rokach-2016"></d-cite>, <d-cite key="weinberger-2018"></d-cite>.</p> <p>It is also possible to employ early stopping or pruning for the individual trees. In particular, decision trees based on a single decision are called decision tree stumps. For the pruning step, instead of growing the tree to its full depth based on data $S_j$, we can use the left-out samples $S \setminus S_j$ as a validation set and post-prune the decision tree. <d-cite key="weinberger-2018"></d-cite></p> <p>An advantage of the random forest is that it only has two hyperparameters: the number of subsampled datasets $M$ and the number of subsampled features $k$; it is very insensitive to both <d-cite key="weinberger-2018"></d-cite>.</p> <h2 id="extremely-randomized-trees">Extremely Randomized Trees</h2> <p>Extremely randomized trees (Extra-Trees) introduce a high degree of randomization in selecting splits for both attributes and cut points. Unlike conventional methods that seek to find the optimal split based on specific criteria (such as information gain), Extra-Trees selects these splits totally or partially at random. The extreme case is that the structure of the built tree does not correlate at all with the labels of the training set. The benefit of this method is that it is computationally very efficient to implement. <d-cite key="geurts-2006"></d-cite></p> <p>Traditional tree models are significantly influenced by the randomness of the learning sample, leading to a high variance in the model outcomes. The Extra-Trees algorithm aims to leverage this randomness as a core component of the model-building process. By selecting splits randomly, the method seeks to reduce the variance associated with the choice of cut points, which has been identified as a significant contributor to the error rates in tree-based methods. In addition, this algorithm is computationally very efficient despite the necessity of growing multiple models. <d-cite key="rokach-2016"></d-cite> <d-cite key="geurts-2006"></d-cite></p> <p>The ensemble model for the Extra-Trees has two parameters: $k$, the number of features randomly selected for each node, and the early stopping criterion $n_\mathrm{min}$ that is, if the subset $S$ has strictly fewer samples than $n_\mathrm{min}$ the algorithm terminates. In addition, the algorithm does not use bootstrapped sub-samples as for the random forest but instead considers the whole input data set $M$ times. As usual, the final classification is determined using a majority vote. <d-cite key="geurts-2006"></d-cite></p> <p>The corresponding algorithm to build and ExtraTree is defined using that for a given set $S$, $\operatorname{values}_S(i) = \operatorname{values} \{ \mathbf{x} \in \mathcal{S} \mid \mathbf{x}_i \}$ is the set of all possible values of the i’th feature in $\mathcal{S}$.</p> <p><strong>Extra-Tree Training Algorithm</strong>, <em>adapted from <d-cite key="geurts-2006"></d-cite></em></p> \[\begin{array}{l} \textbf{Input:} &amp;\text{Training set } S \subseteq \mathcal{D} = \{(\mathbf{x}_i, y_i) \mid \mathbf{x}_i \in \mathbb{R}^d, y_i \in \{-1, 1\}\}_{i=1}^n \\ \textbf{Output:} &amp;\text{Decision tree} \end{array}\] \[\begin{array}{l} \textbf{Procedure: } \text{Extra-Trees}(S) \\ \quad \textbf{if } |S| &lt; n_\text{min} \textbf{ or all } y_i \text{ in } S \text{ are identical or all } \vec{x}_i \text{ in } S \text{ are identical} \\ \quad \quad \textbf{return } \operatorname{Leaf}(\operatorname{mode}(Y)), \text{ where } Y = \{y_1, \ldots, y_n\} \text{ and ties are broken randomly} \\ \quad \textbf{else} \\ \quad \quad \text{Sample a random subset of features } I \subseteq [d] \text{ of size } |I| = k \leq d \text{ without replacement} \\ \quad \quad \textbf{for each attribute } i \text{ in } I \textbf{ do} \\ \quad \quad \quad s_i = \text{Pick a Random Split}(S, i) \\ \quad \quad \textbf{end for} \\ \quad \quad \text{Find } s^\star \text{ such that } s^\star = \underset{i=1, \dots, k}{\arg \max} \operatorname{InfoGain}(S,s_i) \\ \quad \quad \text{Split } S \text{ into subsets } S_{\text{left}} \text{ and } S_{\text{right}} \text{ according to split } s^\star \\ \quad \quad T_{\text{left}} \leftarrow \text{Extra-Tree}(S_{\text{left}}) \\ \quad \quad T_{\text{right}} \leftarrow \text{Extra-Tree}(S_{\text{right}}) \\ \quad \quad \textbf{return } \text{tree with decision node on } s^\star, \text{ left branch } T_{\text{left}}, \text{ and right branch } T_{\text{right}} \\ \quad \textbf{end if} \end{array}\] \[\begin{array}{l} \textbf{Input: } &amp;\text{Whole Training Set } \mathcal{D}, \text{ Training Set } S \subseteq \mathcal{D}, \text{ Feature } i \\ \textbf{Output: } &amp; \text{Split } s_i \text{ on feature } i \\ \end{array}\] \[\begin{array}{l} \textbf{Procedure: } \text{Pick a Random Split}(S, i) \\ \quad \text{Let } \mathcal{A}_\mathcal{D} = \operatorname{values}_{\mathcal{D}}(i) \text{ be the set of all possible values for } i \text{ in } \mathcal{D} \\ \quad \text{Let } \mathcal{A}_S = \operatorname{values}_S(i) \subseteq \mathcal{A}_\mathcal{D}, \text{ i.e., the subset of } \mathcal{A}_\mathcal{D} \text{ values that appears in } S \\ \quad \text{Randomly draw a proper non-empty subset } \mathcal{A}_1 \subset \mathcal{A}_S \text{ and a subset } \mathcal{A}_2 \subset \mathcal{A}_\mathcal{D} \setminus \mathcal{A}_S \\ \quad \textbf{return } \text{split } s_i \in \mathcal{A}_1 \cup \mathcal{A}_2 \text{ on feature } i \end{array}\] <h2 id="gradient-boosting">Gradient Boosting</h2> <p>The main idea behind boosting is to go from a weak classifier to a strong classifier. Given a current ensemble, one way to approach this problem is to train another classifier where misclassified samples are given a higher importance. This is known as Adaboost <d-cite key="adaboost"></d-cite>. Instead of giving misclassified samples higher importance, you can also sequentially train new classifiers to approximate the residuals of the previous ensemble. Adding them to the previous ensemble will “correct” it and is known as gradient boosting. This section presents gradient boosting based on the work in <d-cite key="gbm_orig"></d-cite>.</p> <h3 id="formal-definition">Formal Definition</h3> <p>Our goal is to find a function $\hat{F} \in \mathcal{F}$ that best approximates $y$ given $x$. A function $F \in \mathcal{F}$ is of the form for some $M \in \mathbb{N}$:</p> \[F(x) = \sum_{m=1}^M h_m(x) \gamma_m + c = \sum_{m=0}^M \gamma_m h_m(x)\] <p>For simplicity of notation, define $\gamma_0 h_0(x) = c$. We have that $\gamma_1, \dots, \gamma_n, c \in \mathbb{R}$ are constants and $h_1, …, h_m \in \mathcal{H}$ are known as base learners. For example, these base learners could be decision trees or linear regression models.</p> <p>We will define:</p> \[\begin{align} &amp;F_0(x) = c = \underset{a}{\arg \min } \mathbb{E}_{x, y} \left[\mathcal{L}(y, a)\right] \end{align}\] <p>We further define for $m \geq 1$ and some differentiable loss function $\mathcal{L}$:</p> \[\begin{align} r_m &amp;= - \nabla_{F_{m-1}} \mathcal{L}(y, F_{m-1}(x)) \label{math_res}\\ h_m &amp;= \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y}\left[(r_m - h(x))^2\right] \label{math_h} \\ \gamma_m &amp;= \underset{\gamma}{\arg \min } \mathbb{E}_{x, y}[\mathcal{L}\left(y, F_{m-1}(x) + \gamma h_m(x)\right)] \label{math_gamma} \\ F_m(x) &amp;= F_{m-1}(x) + \gamma_m h_m(x) \label{math_F} \end{align}\] <p>In the literature, $r_m$ is generally referred to as the pseudo-residual. For a given parameter $M \in \mathbb{N}$, we will define our approximation $\hat{F}(x) = F_M(x)$.</p> <h3 id="connection-to-steepest-gradient-descent">Connection to Steepest Gradient Descent</h3> <p>Consider the following optimization problem:</p> \[\begin{align} &amp;h_m=\underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y} \left[\mathcal{L}\left(y, F_{m-1}\left(x\right)+h\left(x\right)\right)\right] \label{math_opt_orig} \end{align}\] <p>For a general function class $\mathcal{H}$, the optimization problem in equation \(\ref{math_opt_orig}\) is infeasible.</p> <p>As we have seen in the lecture, we can find a local minimum of a function with steepest gradient descent <d-cite key="gd_steepest"></d-cite>. Given a function $F_{m-1} \in \mathcal{F}$, an iteration of steepest gradient descent with the aim of minimizing $\mathcal{L}$ yields:</p> \[\begin{align} \hat{\gamma}_m = \underset{\gamma}{\arg \min } \mathbb{E}_{x, y}\left[\mathcal{L}\left(y, F_{m-1}(x) - \gamma \nabla_{F_{m-1}} \mathcal{L}(y, F_{m-1}(x)) \right) \right]\\ \hat{F}_m(x) = F_{m-1}(x) - \gamma_m \mathbb{E}_{x, y}\left[\nabla_{F_{m-1}} \mathcal{L}(y, F_{m-1}(x))\right] \end{align}\] <p>By the properties of steepest gradient descent, we have</p> \[\mathbb{E}_{x, y}\left[\mathcal{L}(y, \hat{F}_m(x))\right] \leq \mathbb{E}_{x, y}\left[\mathcal{L}(y, F_{m-1}(x))\right]\] <p>However, we do not necessarily have \(\hat{F}_m \in \mathcal{F}\) given that $F_{m-1} \in \mathcal{F}$.</p> <p>Equation \(\ref{math_h}\) defines an approximation in $\mathcal{H}$ of \(- \mathbb{E}_{x, y}\left[\nabla_{F_{m-1}} \mathcal{L}(y, F_{m-1}(x)) \right]\). Consequently, we can understand gradient boosting as an approximation of the steepest gradient descent method, where we approximate the gradient by some function $h \in \mathcal{H}$.</p> <h3 id="algorithm-for-a-finite-dataset">Algorithm for a finite Dataset</h3> <p>Consider a finite dataset $\mathcal{D} = \{ (x_i, y_i) \mid i = 1, \dots, n \} $:</p> <p><strong>Gradient Boosting</strong>, <em>adapted from <d-cite key="gbm_orig"></d-cite></em> \</p> \[\begin{array}{l} F_0(x) = \underset{a}{\arg \min } \mathbb{E}_{y}[\mathcal{L}(y, a)] \\ \textbf{for } m = 1 \text{ to } M \textbf{ do} \\ \quad r_i = - \nabla_{F_{m-1}} \mathcal{L}(y_i, F_{m-1}(x_i)) \text{ for } i=1,\dots, n \\ \quad h_m = \underset{h \in \mathcal{H}}{\arg \min } \sum_{i=1}^n (r_i - h(x_i))^2 \\ \quad \gamma_m = \underset{\gamma}{\arg \min } \sum_{i=1}^n \mathcal{L}(y_i, F_{m-1}(x_i) + \gamma h(x_i)) \\ \quad F_m(x) = F_{m-1}(x) + \gamma_m h_m(x) \\ \textbf{end for} \\ \textbf{return } F_M(x) \end{array}\] <h3 id="regularization">Regularization</h3> <p>Compared to the random forest, boosting iteratively “corrects” ensemble models over the course of training. Although this might lead to higher performance on the dataset, this might hurt generalization since we might overfit the training data by “over-correcting” for it. This becomes especially problematic if our training data is noisy or even contains outliers; since then, we will be fitting the noise. Consequently, modern implementations <d-cite key="scikit-learn"></d-cite>, <d-cite key="lgbm"></d-cite>, <d-cite key="xgboost"></d-cite> have specific parameters for regularization.</p> <h4 id="regularize-base-learners">Regularize Base Learners</h4> <p>Similar to other machine learning methods, we can add a regularization parameter $\Omega$ that penalizes the complexity of the model. For example, we could adapt the definition of $h_m$ as follows:</p> \[h_m = \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y}\left[(r - h(x))^2\right] + \Omega(h)\] <h4 id="early-stopping">Early Stopping</h4> <p>Early Stopping is a method that is used to estimate the number of weak learners required for training. Before training the model, we set aside a part of the training data, which we will refer to as the validation data. We evaluate our model on this validation set by adding new estimators during training. As soon as the performance on the validation set decreases or does not improve, we will stop training. This means that no new estimators are added.</p> <h4 id="shrinkagelearning-rate">Shrinkage/Learning Rate</h4> <p>We can add a parameter $\nu$, which scales the contribution of each weak learner. The update in equation \(\ref{math_opt_orig}\) will be changed to:</p> \[F_m(x) = F_{m-1}(x) + \nu \cdot \gamma_m h_m(x)\] <p>In literature <d-cite key="gbm_orig"></d-cite>, this parameter is called shrinkage. However, many implementations, such as the one from <code class="language-plaintext highlighter-rouge">scikit-learn</code> <d-cite key="scikit-learn"></d-cite>, call this parameter <code class="language-plaintext highlighter-rouge">learning_rate</code> instead. Similar to gradient descent, where a lower learning rate will result in slower convergence, we will need more estimators to converge. Empirical results have shown that $\nu \leq 0.1$ combined with early stopping achieves the best results <d-cite key="scikit-learn"></d-cite>.</p> <h4 id="subsampling">Subsampling</h4> <p>Stochastic Gradient Boosting <d-cite key="gbm_stochastic"></d-cite> is a minor modification to the algorithm. It consists of training each base learner only on a subsample of the training data. In <code class="language-plaintext highlighter-rouge">scikit-learn</code> <d-cite key="scikit-learn"></d-cite>, this parameter is called <code class="language-plaintext highlighter-rouge">subsample</code>. We have that $0 &lt;$ <code class="language-plaintext highlighter-rouge">subsample</code> $\leq 1$.</p> <h3 id="gradient-tree-boosting">Gradient tree boosting</h3> <p>Consider the case where we use decision trees as base learners. $h_m$ is of the following form:</p> \[h_m(x) = \sum_{j=1}^{J_m} b_{j,m} \mathbb{1}_{R_{j,m}}(x)\] <p>The decision tree splits the input space into $J_m$ distinct regions $R_{1,m},\dots,R_{J_m,m}$. Furthermore, $b_{j,m}$ denotes the value predicted in the $R_{j,m}$ region. \(\mathbb{1}_{R_{j,m}}(x)\) is the indicator value for $x \in R_{j,m}$.</p> <h4 id="improved-optimization">Improved Optimization</h4> <p>Another version <d-cite key="gbm_orig"></d-cite> alters equation \(\ref{math_gamma}\) and \(\ref{math_F}\) to:</p> \[\begin{align} &amp; \gamma_{j,m} = \underset{\gamma}{\arg \min } \mathbb{E}_{x \in R_{j,m}, y}[\mathcal{L}\left(y, F_{m-1}(x) + \gamma h_m(x)\right)] \\ &amp; F_m(x) = F_{m-1}(x) + \sum_{j=1}^{J_m} \gamma_{j,m} \mathbb{1}_{R_{j,m}}(x) \end{align}\] <p>In essence, we have a separate error correction for each region. This is guaranteed to not give a worse solution on a single iteration since the solution to the original optimization problem considers the same exact problem but with the constraint of $\gamma_{1,m} = \dots = \gamma_{J_m,m}$.</p> <h4 id="histogram-gradient-boosting">Histogram Gradient Boosting</h4> <p>By the iterative design of gradient boosting, the parallelization of the training process is much harder compared to the random forest. In the case of the random forest, we can naturally build multiple trees in parallel, whereas this is not possible in the case of boosting. This limited the use of the classical gradient boosting algorithm on large datasets since the training process took too long. Histogram Gradient Boosting can be used to increase the speed of building individual trees.</p> <p>The main bottleneck in building a decision tree is finding the optimal split. This requires us to compute a metric for all features and feature values, which requires $\mathcal{O}(\text{#features} \times n \log n)$ time since we need to sort all values of all our features.</p> <p>Although we cannot increase the sorting speed, we can approximate the optimal split by quantizing each feature. This is done by splitting the data values of each feature into bins. These bins are equal-density histograms, meaning all the intervals contain the same number of values. The quantized value of the feature will be the index of the interval. This index is chosen so that the ordinal order is preserved between the values of features in different intervals. Here is an example for the case where we have three bins, four samples, and two features <d-cite key="histogram_gbm"></d-cite>:</p> \[\begin{align*} \begin{bmatrix} 1.5 &amp; 0.0\\ 0.0 &amp; 5.5\\ 0.3 &amp; 7.0\\ 5.5 &amp; 8.5 \end{bmatrix} \quad\rightarrow\quad \begin{bmatrix} 1 &amp; 0\\ 0 &amp; 1\\ 0 &amp; 2\\ 2 &amp; 2 \end{bmatrix} \end{align*}\] <p>The best split is then computed by not considering the feature values but the histogram interval values. Since the number of intervals is a constant value and the indexes are chosen to be the values \(1, 2, \dots, \#\text{intervals}\), we are not required to sort the values in order to compute the gain. This results in a runtime of $\mathcal{O}(\text{#features} \times \text{#intervals})$.</p> <h2 id="xgboost">XGBoost</h2> <p>XGBoost is an open-source implementation of regularized Gradient Boosting Machines <d-cite key="xgboost"></d-cite>. It has been and is still being used extensively in machine learning competitions, where it is commonly part of the winning solution. However, many people use this model as a black box without properly understanding the underlying principles. While discussing all of the implementation details of XGBoost would go beyond this blog post, this section aims to give a broad overview of the main ideas behind XGBoost <d-cite key="xgboost"></d-cite>.</p> <h3 id="second-order-gradient-boosting">Second Order Gradient Boosting</h3> <p>XGBoost is based on the ideas behind gradient boosting. However, in contrast to the standard algorithm described in the previous section, XGBoost considers Gradients and Hessians.</p> <h3 id="formal-definition-1">Formal Definition</h3> <p>Similar to gradient boosting, our goal is to find a function $\hat{F} \in \mathcal{F}$ that best approximates $y$ given $x$. A function $F \in \mathcal{F}$ is of the form for some $M \in \mathbb{N}$:</p> \[F(x) = \sum_{m=1}^M h_m(x) \gamma_m + c = \sum_{m=0}^M \gamma_m h_m(x)\] <p>For simplicity of notation, define $\gamma_0 h_0(x) = c$. We have that \(\gamma_1, \dots, \gamma_n, c \in \mathbb{R}\) are constants and $h_1, \dots, h_m \in \mathcal{H}$ are known as base learners.</p> <p>We will define:</p> \[F_0(x) = c = \underset{a}{\arg \min } \mathbb{E}_{x, y} [\mathcal{L}(y, a)]\] <p>We further define for $m \geq 1$ and some twice differentiable loss function $\mathcal{L}$:</p> \[\begin{align} g &amp;= \nabla_{F_{m-1}(x)} \mathcal{L}(y, F_{m-1}(x)) \label{math_res_2}\\ H &amp;= \nabla^2_{F_{m-1}(x)} \mathcal{L}(y, F_{m-1}(x)) \label{math_res_2H}\\ h_m &amp;= \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y}\left[\frac{H}{2} \left(\frac{g}{H} - h(x)\right)^2\right] \label{math_h_2}\\ F_m(x) &amp;= F_{m-1}(x) + h_m(x) \label{math_F_2} \end{align}\] <p>For a given parameter $M \in \mathbb{N}$, we will define our approximation $\hat{F}(x) = F_M(x)$.</p> <h3 id="a-second-order-approximation-of-mathcall">A second order approximation of $\mathcal{L}$</h3> <p>Consider the following optimization problem:</p> \[\begin{align} &amp;h_m= \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y} \left[\mathcal{L}\left(y, F_{m-1}\left(x\right)+h\left(x\right)\right)\right] \label{math_opt_orig_2} \end{align}\] <p>For a general function class $\mathcal{H}$, the optimization problem in equation \(\ref{math_opt_orig_2}\) is infeasible. We will use the second-order Taylor approximation of the loss function.</p> \[\begin{align} &amp; \mathcal{L}\left(y, F_{m-1}\left(x\right)+h\left(x\right)\right) \approx \mathcal{L}(y, F_{m-1}(x)) + g h(x) + \frac{1}{2} H h(x)^2 \end{align}\] <p>The optimization problem for this approximation can be rewritten as follows:</p> \[\begin{align} h_m &amp;= \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y} \left[\mathcal{L}(y, F_{m-1}(x)) + g h(x) + \frac{1}{2} H h(x)^2\right] \\ &amp;= \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y} \left[g h(x) + \frac{1}{2} H h(x)^2 + \frac{1}{2}g^2\right] \\ &amp;= \underset{h \in \mathcal{H}}{\arg \min } \mathbb{E}_{x, y} \left[\frac{H}{2} \left(\frac{g}{H} - h(x)\right)^2 \right] \end{align}\] <p>This is exactly the same optimization problem as in equation \(\ref{math_h_2}\). It is worth mentioning that XGBoost adds regularization. This is done by adding $\Omega(h)$ to the minimization problem for $h_m$, which penalizes complexity. Furthermore, <a href="#shrinkage">Shrinkage/Learning Rate</a> from Gradient Boosting and feature subsampling from the Random Forest are used.</p> <h3 id="algorithm-for-a-finite-dataset-1">Algorithm for a finite Dataset</h3> <p><strong>Second Order Gradient Boosting</strong>, <em>Adapted from <d-cite key="xgboost"></d-cite></em></p> <p>Consider a finite dataset $\mathcal{D} = \{(x_i, y_i) \mid i = 1, \dots, n\}$:</p> \[\begin{array}{l} F_0(x) = \underset{a}{\arg \min} \sum_{i=1}^n \mathcal{L}(y_i, a) \\ \textbf{for } m = 1 \text{ to } M \textbf{ do} \\ \quad g_i = \nabla_{F_{m-1}} \mathcal{L}(y, F_{m-1}(x)) \text{ for } i = 1, \dots, n \\ \quad H_i = \nabla^2_{F_{m-1}} \mathcal{L}(y, F_{m-1}(x)) \text{ for } i = 1, \dots, n \\ \quad h_m = \underset{h \in \mathcal{H}}{\arg \min } \sum_{i=1}^n \frac{H_i}{2}\left(\frac{g_i}{H_i} - h(x_i) \right)^2 \\ \quad F_m(x) = F_{m-1}(x) + h_m(x) \\ \textbf{end for} \\ \textbf{return } F_M(x) \end{array}\] <h3 id="weighted-quantile-sketch">Weighted Quantile Sketch</h3> <p>For tree building, XGBoost uses an approximate solution that shares some similarities with <a href="#histogram-gradient-boosting">Histogram Gradient Boosting</a>. The main goal is to reduce the time needed to find the optimal split. Consider the multi-set \(\mathcal{D}_k = \{(x_{1,k}, H_1), (x_{2,k}, H_2), \ldots, (x_{n,k}, H_n)\}\), where $x_{i,k}$ is the $i$’th feature value of the $k$’th feature and $H_i$ is equation \(\ref{math_F_2}\) evaluated at point $x_i$. We will define the following rank function:</p> \[r_k(z) = \frac{1}{\sum_{(x,H) \in \mathcal{D}_k} H} \sum_{(x, H) \in \mathcal{D}_k, x &lt; z} H\] <p>Intuitively, this ranking function can be interpreted as the data points weighted by $H_i$. This happens in equation \(\ref{math_h_2}\) as well, which is nothing else but a weighted sum of the squared loss with labels $\frac{g_i}{H_i}$ and weights $H_i$.</p> <p>A candidate split for $\epsilon &gt; 0$ of the $k$’th feature is defined as the data points \(\{s_{k,1}, \dots, s_{k,l}\}\), such that</p> \[|r_k(s_{k,j}) - r_k(s_{k,j+1})| &lt; \epsilon, \quad s_{k,1} = \min_i x_{i,k}, \quad s_{k,l} = \max_i x_{i, k}\] <p>Finding such a candidate split is called the weighted quantile sketch problem, which is non-trivial for large datasets. The authors of <d-cite key="xgboost"></d-cite> propose an algorithm to approximate the problem. This algorithm has some nice theoretical properties. The proofs and description of the algorithm can be found in the appendix of <d-cite key="xgboost"></d-cite>.</p> <h3 id="sparsity-awareness">Sparsity Awareness</h3> <p>Many real-world datasets are sparse, which is generally caused by missing values in the data, frequent zero values in statistics, and artifacts from feature engineering (e.g., one-hot encoding).</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/ensemble-methods/xgboost_sparsity-480.webp 480w,/assets/img/ensemble-methods/xgboost_sparsity-800.webp 800w,/assets/img/ensemble-methods/xgboost_sparsity-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/ensemble-methods/xgboost_sparsity.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><em>Figure: Handling Missing Values. Reproduced from <d-cite key="xgboost"></d-cite></em></p> <p>XGBoost adds a default direction for missing values in the decision tree. In order to generate a new split for a feature, only the non-missing values are considered. However, the split is computed for both cases where the missing values either all go to the left or the right direction. The optimal split and the default direction for the missing values are chosen based on the maximum gain. For certain very sparse datasets, this can lead to a 50x improvement in runtime <d-cite key="xgboost"></d-cite> compared to a basic solution such as imputation.</p> <h3 id="system-design">System Design</h3> <p>Here are some features of XGBoost with regard to System Design. For more details, see <d-cite key="xgboost"></d-cite>.</p> <ul> <li>Parallelization</li> <li>Cache-aware Computation, which results in a 2x improvement in runtime for very large datasets <d-cite key="xgboost"></d-cite></li> <li>Distributed Training on a Cluster</li> <li>Support for processing large datasets that do not fit onto the main disk</li> </ul> <h2 id="experiment">Experiment</h2> <p>In this section, we present the results of Random Forest, Extra Trees, Gradient Boosting, and XGBoost on real-world data. We use the implementations of <code class="language-plaintext highlighter-rouge">scikit-learn</code> <d-cite key="scikit-learn"></d-cite> and <code class="language-plaintext highlighter-rouge">xgboost</code> <d-cite key="xgboost"></d-cite> for our experiments; the corresponding implementation can be found in <a href="#code-for-experiment">Code for Experiment</a>. This section demonstrates the practical ease of use of these algorithms. This often leads to them being used as a black box.</p> <h3 id="california-housing">California Housing</h3> <p>We use the <code class="language-plaintext highlighter-rouge">California Housing</code> dataset from <code class="language-plaintext highlighter-rouge">scikit-learn</code> <d-cite key="scikit-learn"></d-cite>. This dataset consists of only eight numeric features and is a regression task consisting of predicting a house price given these features. We perform 3-fold cross-validation on the dataset and measure runtime &amp; <code class="language-plaintext highlighter-rouge">R2 Score</code>. Furthermore, we train multiple instances of each classifier, where we modify the maximum number of estimators each method uses. The error bars denote the difference in runtime between different folds.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/ensemble-methods/xgboost_experiment-480.webp 480w,/assets/img/ensemble-methods/xgboost_experiment-800.webp 800w,/assets/img/ensemble-methods/xgboost_experiment-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/ensemble-methods/xgboost_experiment.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><em>Figure: Runtime and R2 Score with different number of trees. The left figure presents an overview of all evaluated models, whereas the right excludes the Gradient Boosting model to enhance readability.</em></p> <p>As one can clearly see, the runtime of standard gradient boosting is significantly longer compared to the other methods. This can be explained by the fact that the iterative tree building of gradient boosting is hard to parallelize. Since the experiments were run on a machine with 128 cores, this significantly impacts runtime. Another observation that can be made is that the Random Forest and Extra Trees both plateau much earlier than the gradient methods while having the shortest runtime.</p> <h2 id="appendix">Appendix</h2> <h3 id="code-for-experiment">Code for Experiment</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># This code has been adapted from:
# https://scikit-learn.org/stable/auto_examples/ensemble/plot_forest_hist_grad_boosting_comparison.html
</span>
<span class="kn">from</span> <span class="n">sklearn.datasets</span> <span class="kn">import</span> <span class="n">fetch_california_housing</span>

<span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="o">=</span> <span class="nf">fetch_california_housing</span><span class="p">(</span><span class="n">return_X_y</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">as_frame</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="n">n_samples</span><span class="p">,</span> <span class="n">n_features</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">shape</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">The dataset consists of </span><span class="si">{</span><span class="n">n_samples</span><span class="si">}</span><span class="s"> samples and </span><span class="si">{</span><span class="n">n_features</span><span class="si">}</span><span class="s"> features</span><span class="sh">"</span><span class="p">)</span>

<span class="kn">import</span> <span class="n">joblib</span>

<span class="n">N_CORES</span> <span class="o">=</span> <span class="mi">128</span><span class="c1">#joblib.cpu_count(only_physical_cores=True)
</span><span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Number of physical cores: </span><span class="si">{</span><span class="n">N_CORES</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>

<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeRegressor</span>
<span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">HistGradientBoostingRegressor</span><span class="p">,</span> <span class="n">RandomForestRegressor</span><span class="p">,</span> <span class="n">GradientBoostingRegressor</span><span class="p">,</span> <span class="n">ExtraTreesRegressor</span>
<span class="kn">from</span> <span class="n">xgboost</span> <span class="kn">import</span> <span class="n">XGBRegressor</span>
<span class="kn">from</span> <span class="n">sklearn.model_selection</span> <span class="kn">import</span> <span class="n">GridSearchCV</span><span class="p">,</span> <span class="n">KFold</span>

<span class="n">models</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">Extra Trees</span><span class="sh">"</span><span class="p">:</span> <span class="nc">ExtraTreesRegressor</span><span class="p">(</span>
        <span class="n">random_state</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">n_jobs</span><span class="o">=</span><span class="n">N_CORES</span><span class="p">,</span> <span class="c1">#max_depth=8, min_samples_leaf=5, min_samples_split=3, min_weight_fraction_leaf=0.1,
</span>    <span class="p">),</span>
    <span class="c1">#"Decision Tree" : DecisionTreeRegressor(
</span>    <span class="c1">#   random_state=0
</span>    <span class="c1">#),
</span>    <span class="sh">"</span><span class="s">XGBoost</span><span class="sh">"</span> <span class="p">:</span> <span class="nc">XGBRegressor</span><span class="p">(</span>
        <span class="n">random_state</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">learning_rate</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span> <span class="n">max_depth</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
    <span class="p">),</span>
    <span class="sh">"</span><span class="s">Gradient Boosting</span><span class="sh">"</span> <span class="p">:</span> <span class="nc">GradientBoostingRegressor</span><span class="p">(</span>
        <span class="n">random_state</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">max_depth</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span> <span class="n">learning_rate</span><span class="o">=</span><span class="mf">0.2</span><span class="p">,</span> <span class="n">min_samples_split</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">min_samples_leaf</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">min_weight_fraction_leaf</span><span class="o">=</span><span class="mf">0.1</span>
    <span class="p">),</span>
    <span class="sh">"</span><span class="s">Random Forest</span><span class="sh">"</span><span class="p">:</span> <span class="nc">RandomForestRegressor</span><span class="p">(</span>
        <span class="n">random_state</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">n_jobs</span><span class="o">=</span><span class="n">N_CORES</span><span class="p">,</span> <span class="c1">#max_depth=10, min_samples_leaf=2, min_samples_split=5, min_weight_fraction_leaf=0.1
</span>    <span class="p">),</span>

    <span class="c1">#"Hist Gradient Boosting": HistGradientBoostingRegressor(
</span>    <span class="c1">#    max_leaf_nodes=15, random_state=0, early_stopping=False
</span>    <span class="c1">#),
</span><span class="p">}</span>
<span class="n">n_estimators</span> <span class="o">=</span> <span class="p">[</span><span class="mi">50</span><span class="p">,</span> <span class="mi">100</span><span class="p">,</span> <span class="mi">150</span><span class="p">,</span> <span class="mi">200</span><span class="p">,</span> <span class="mi">250</span><span class="p">,</span> <span class="mi">300</span><span class="p">,</span> <span class="mi">350</span><span class="p">,</span> <span class="mi">400</span><span class="p">,</span> <span class="mi">450</span><span class="p">,</span> <span class="mi">500</span><span class="p">]</span>
<span class="n">max_depth</span> <span class="o">=</span> <span class="p">[</span><span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">7</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">10</span><span class="p">]</span>
<span class="n">learning_rate</span> <span class="o">=</span> <span class="p">[</span><span class="mf">0.01</span><span class="p">,</span> <span class="mf">0.05</span><span class="p">,</span> <span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.15</span><span class="p">,</span> <span class="mf">0.2</span><span class="p">]</span>
<span class="n">min_samples_split</span> <span class="o">=</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">]</span>
<span class="n">min_samples_leaf</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">]</span>
<span class="n">min_weight_fraction_leaf</span> <span class="o">=</span> <span class="p">[</span><span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.2</span><span class="p">,</span> <span class="mf">0.3</span><span class="p">,</span> <span class="mf">0.4</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">]</span>

<span class="n">param_grids</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">Extra Trees</span><span class="sh">"</span> <span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">n_estimators</span><span class="sh">"</span><span class="p">:</span> <span class="n">n_estimators</span><span class="p">,</span>
        <span class="c1">#'max_depth' : max_depth,
</span>        <span class="c1">#'min_samples_split' : min_samples_split,
</span>        <span class="c1">#'min_samples_leaf' : min_samples_leaf,
</span>        <span class="c1">#'min_weight_fraction_leaf' : min_weight_fraction_leaf
</span>    <span class="p">},</span>
    <span class="sh">"</span><span class="s">XGBoost</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">n_estimators</span><span class="sh">"</span><span class="p">:</span> <span class="n">n_estimators</span><span class="p">,</span>
        <span class="c1">#'max_depth' : max_depth,
</span>        <span class="c1">#'learning_rate' : learning_rate,
</span>        <span class="c1">#'min_samples_split' : min_samples_split,
</span>        <span class="c1">#'min_samples_leaf' : min_samples_leaf,
</span>        <span class="c1">#'min_weight_fraction_leaf' : min_weight_fraction_leaf
</span>    <span class="p">},</span>
    <span class="sh">"</span><span class="s">Gradient Boosting</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">n_estimators</span><span class="sh">"</span><span class="p">:</span> <span class="n">n_estimators</span><span class="p">,</span>
        <span class="c1">#'max_depth' : max_depth,
</span>        <span class="c1">#'learning_rate' : learning_rate,
</span>        <span class="c1">#'min_samples_split' : min_samples_split,
</span>        <span class="c1">#'min_samples_leaf' : min_samples_leaf,
</span>        <span class="c1">#'min_weight_fraction_leaf' : min_weight_fraction_leaf
</span>    <span class="p">},</span>
    <span class="sh">"</span><span class="s">Random Forest</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">n_estimators</span><span class="sh">"</span><span class="p">:</span> <span class="n">n_estimators</span><span class="p">,</span>
        <span class="c1">#'max_depth' : max_depth,
</span>        <span class="c1">#'min_samples_split' : min_samples_split,
</span>        <span class="c1">#'min_samples_leaf' : min_samples_leaf,
</span>        <span class="c1">#'min_weight_fraction_leaf' : min_weight_fraction_leaf
</span>    <span class="p">},</span>
    <span class="sh">"</span><span class="s">Hist Gradient Boosting</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">max_iter</span><span class="sh">"</span><span class="p">:</span> <span class="n">n_estimators</span>
    <span class="p">},</span>
<span class="p">}</span>

<span class="n">cv</span> <span class="o">=</span> <span class="nc">KFold</span><span class="p">(</span><span class="n">n_splits</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">shuffle</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="n">results</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">model</span> <span class="ow">in</span> <span class="n">models</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">name</span><span class="p">)</span>
    <span class="n">grid_search</span> <span class="o">=</span> <span class="nc">GridSearchCV</span><span class="p">(</span>
        <span class="n">estimator</span><span class="o">=</span><span class="n">model</span><span class="p">,</span>
        <span class="n">param_grid</span><span class="o">=</span><span class="n">param_grids</span><span class="p">[</span><span class="n">name</span><span class="p">],</span>
        <span class="n">return_train_score</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">cv</span><span class="o">=</span><span class="n">cv</span><span class="p">,</span>
        <span class="n">n_jobs</span><span class="o">=</span><span class="mi">1</span> <span class="k">if</span> <span class="n">name</span> <span class="o">!=</span> <span class="sh">"</span><span class="s">Gradient Boosting</span><span class="sh">"</span> <span class="k">else</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="c1"># train multiple models if gradient boosting since it will use at most 1 core anyway. For the other methods, only train 1 model at a time in order to use all cores for training a single model
</span>    <span class="p">).</span><span class="nf">fit</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">)</span>
    <span class="n">result</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="n">name</span><span class="p">,</span> <span class="sh">"</span><span class="s">cv_results</span><span class="sh">"</span><span class="p">:</span> <span class="n">pd</span><span class="p">.</span><span class="nc">DataFrame</span><span class="p">(</span><span class="n">grid_search</span><span class="p">.</span><span class="n">cv_results_</span><span class="p">)}</span>
    <span class="n">results</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>

    <span class="kn">import</span> <span class="n">plotly.colors</span> <span class="k">as</span> <span class="n">colors</span>
<span class="kn">import</span> <span class="n">plotly.express</span> <span class="k">as</span> <span class="n">px</span>
<span class="kn">from</span> <span class="n">plotly.subplots</span> <span class="kn">import</span> <span class="n">make_subplots</span>

<span class="n">fig</span> <span class="o">=</span> <span class="nf">make_subplots</span><span class="p">(</span>
    <span class="n">rows</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
    <span class="n">cols</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
    <span class="n">subplot_titles</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">Train time vs score</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">vertical_spacing</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span>
    <span class="n">row_heights</span><span class="o">=</span><span class="p">[</span><span class="mf">5.0</span><span class="p">]</span>
<span class="p">)</span>

<span class="n">model_names</span> <span class="o">=</span> <span class="p">[</span><span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span> <span class="k">for</span> <span class="n">result</span> <span class="ow">in</span> <span class="n">results</span><span class="p">]</span>
<span class="n">colors_list</span> <span class="o">=</span> <span class="n">colors</span><span class="p">.</span><span class="n">qualitative</span><span class="p">.</span><span class="n">Plotly</span> <span class="o">*</span> <span class="p">(</span>
    <span class="nf">len</span><span class="p">(</span><span class="n">model_names</span><span class="p">)</span> <span class="o">//</span> <span class="nf">len</span><span class="p">(</span><span class="n">colors</span><span class="p">.</span><span class="n">qualitative</span><span class="p">.</span><span class="n">Plotly</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>
<span class="p">)</span>

<span class="k">for</span> <span class="n">idx</span><span class="p">,</span> <span class="n">result</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">results</span><span class="p">):</span>
    <span class="n">cv_results</span> <span class="o">=</span> <span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">cv_results</span><span class="sh">"</span><span class="p">].</span><span class="nf">round</span><span class="p">(</span><span class="mi">3</span><span class="p">)</span>
    <span class="n">model_name</span> <span class="o">=</span> <span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span>
    <span class="n">param_name</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">param_grids</span><span class="p">[</span><span class="n">model_name</span><span class="p">].</span><span class="nf">keys</span><span class="p">())[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">cv_results</span><span class="p">[</span><span class="n">param_name</span><span class="p">]</span> <span class="o">=</span> <span class="n">cv_results</span><span class="p">[</span><span class="sh">"</span><span class="s">param_</span><span class="sh">"</span> <span class="o">+</span> <span class="n">param_name</span><span class="p">]</span>
    <span class="n">cv_results</span><span class="p">[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">model_name</span>

    <span class="n">scatter_fig</span> <span class="o">=</span> <span class="n">px</span><span class="p">.</span><span class="nf">scatter</span><span class="p">(</span>
        <span class="n">cv_results</span><span class="p">,</span>
        <span class="n">x</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_fit_time</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">y</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_test_score</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">error_x</span><span class="o">=</span><span class="sh">"</span><span class="s">std_fit_time</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">error_y</span><span class="o">=</span><span class="sh">"</span><span class="s">std_test_score</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">hover_data</span><span class="o">=</span><span class="n">param_name</span><span class="p">,</span>
        <span class="n">color</span><span class="o">=</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">line_fig</span> <span class="o">=</span> <span class="n">px</span><span class="p">.</span><span class="nf">line</span><span class="p">(</span>
        <span class="n">cv_results</span><span class="p">,</span>
        <span class="n">x</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_fit_time</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">y</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_test_score</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="n">scatter_trace</span> <span class="o">=</span> <span class="n">scatter_fig</span><span class="p">[</span><span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">line_trace</span> <span class="o">=</span> <span class="n">line_fig</span><span class="p">[</span><span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">scatter_trace</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">marker</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">color</span><span class="o">=</span><span class="n">colors_list</span><span class="p">[</span><span class="n">idx</span><span class="p">]))</span>
    <span class="n">line_trace</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">line</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">color</span><span class="o">=</span><span class="n">colors_list</span><span class="p">[</span><span class="n">idx</span><span class="p">]))</span>
    <span class="n">fig</span><span class="p">.</span><span class="nf">add_trace</span><span class="p">(</span><span class="n">scatter_trace</span><span class="p">,</span> <span class="n">row</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">col</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">fig</span><span class="p">.</span><span class="nf">add_trace</span><span class="p">(</span><span class="n">line_trace</span><span class="p">,</span> <span class="n">row</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">col</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>

<span class="n">fig</span><span class="p">.</span><span class="nf">update_layout</span><span class="p">(</span>
    <span class="n">xaxis</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">title</span><span class="o">=</span><span class="sh">"</span><span class="s">Train time (s) - lower is better</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">yaxis</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">title</span><span class="o">=</span><span class="sh">"</span><span class="s">Test R2 score - higher is better</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">legend</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.72</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">0.05</span><span class="p">,</span> <span class="n">traceorder</span><span class="o">=</span><span class="sh">"</span><span class="s">normal</span><span class="sh">"</span><span class="p">,</span> <span class="n">borderwidth</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
    <span class="n">title</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.5</span><span class="p">,</span> <span class="n">text</span><span class="o">=</span><span class="sh">"</span><span class="s">Speed-score trade-off</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">height</span><span class="o">=</span><span class="mi">1000</span><span class="p">,</span>
    <span class="n">width</span><span class="o">=</span><span class="mi">800</span>
<span class="p">)</span>

<span class="n">fig</span><span class="p">.</span><span class="nf">write_image</span><span class="p">(</span><span class="sh">"</span><span class="s">train_time_vs_score.svg</span><span class="sh">"</span><span class="p">)</span>
<span class="n">fig</span><span class="p">.</span><span class="nf">write_image</span><span class="p">(</span><span class="sh">"</span><span class="s">train_time_vs_score.png</span><span class="sh">"</span><span class="p">)</span>
<span class="n">fig</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>


<span class="kn">import</span> <span class="n">plotly.colors</span> <span class="k">as</span> <span class="n">colors</span>
<span class="kn">import</span> <span class="n">plotly.express</span> <span class="k">as</span> <span class="n">px</span>
<span class="kn">from</span> <span class="n">plotly.subplots</span> <span class="kn">import</span> <span class="n">make_subplots</span>

<span class="n">fig</span> <span class="o">=</span> <span class="nf">make_subplots</span><span class="p">(</span>
    <span class="n">rows</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
    <span class="n">cols</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
    <span class="n">subplot_titles</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">Train time vs score</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">vertical_spacing</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span>
    <span class="n">row_heights</span><span class="o">=</span><span class="p">[</span><span class="mf">5.0</span><span class="p">]</span>
<span class="p">)</span>

<span class="n">model_names</span> <span class="o">=</span> <span class="p">[</span><span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span> <span class="k">for</span> <span class="n">result</span> <span class="ow">in</span> <span class="n">results</span><span class="p">]</span>
<span class="n">colors_list</span> <span class="o">=</span> <span class="n">colors</span><span class="p">.</span><span class="n">qualitative</span><span class="p">.</span><span class="n">Plotly</span> <span class="o">*</span> <span class="p">(</span>
    <span class="nf">len</span><span class="p">(</span><span class="n">model_names</span><span class="p">)</span> <span class="o">//</span> <span class="nf">len</span><span class="p">(</span><span class="n">colors</span><span class="p">.</span><span class="n">qualitative</span><span class="p">.</span><span class="n">Plotly</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>
<span class="p">)</span>

<span class="k">for</span> <span class="n">idx</span><span class="p">,</span> <span class="n">result</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">results</span><span class="p">):</span>

    <span class="n">cv_results</span> <span class="o">=</span> <span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">cv_results</span><span class="sh">"</span><span class="p">].</span><span class="nf">round</span><span class="p">(</span><span class="mi">3</span><span class="p">)</span>
    <span class="n">model_name</span> <span class="o">=</span> <span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span>
    <span class="k">if</span> <span class="n">model_name</span> <span class="o">==</span> <span class="sh">'</span><span class="s">Gradient Boosting</span><span class="sh">'</span><span class="p">:</span> <span class="k">continue</span>

    <span class="n">param_name</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">param_grids</span><span class="p">[</span><span class="n">model_name</span><span class="p">].</span><span class="nf">keys</span><span class="p">())[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">cv_results</span><span class="p">[</span><span class="n">param_name</span><span class="p">]</span> <span class="o">=</span> <span class="n">cv_results</span><span class="p">[</span><span class="sh">"</span><span class="s">param_</span><span class="sh">"</span> <span class="o">+</span> <span class="n">param_name</span><span class="p">]</span>
    <span class="n">cv_results</span><span class="p">[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">model_name</span>

    <span class="n">scatter_fig</span> <span class="o">=</span> <span class="n">px</span><span class="p">.</span><span class="nf">scatter</span><span class="p">(</span>
        <span class="n">cv_results</span><span class="p">,</span>
        <span class="n">x</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_fit_time</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">y</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_test_score</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">error_x</span><span class="o">=</span><span class="sh">"</span><span class="s">std_fit_time</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">error_y</span><span class="o">=</span><span class="sh">"</span><span class="s">std_test_score</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">hover_data</span><span class="o">=</span><span class="n">param_name</span><span class="p">,</span>
        <span class="n">color</span><span class="o">=</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">line_fig</span> <span class="o">=</span> <span class="n">px</span><span class="p">.</span><span class="nf">line</span><span class="p">(</span>
        <span class="n">cv_results</span><span class="p">,</span>
        <span class="n">x</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_fit_time</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">y</span><span class="o">=</span><span class="sh">"</span><span class="s">mean_test_score</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="n">scatter_trace</span> <span class="o">=</span> <span class="n">scatter_fig</span><span class="p">[</span><span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">line_trace</span> <span class="o">=</span> <span class="n">line_fig</span><span class="p">[</span><span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">scatter_trace</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">marker</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">color</span><span class="o">=</span><span class="n">colors_list</span><span class="p">[</span><span class="n">idx</span><span class="p">]))</span>
    <span class="n">line_trace</span><span class="p">.</span><span class="nf">update</span><span class="p">(</span><span class="n">line</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">color</span><span class="o">=</span><span class="n">colors_list</span><span class="p">[</span><span class="n">idx</span><span class="p">]))</span>
    <span class="n">fig</span><span class="p">.</span><span class="nf">add_trace</span><span class="p">(</span><span class="n">scatter_trace</span><span class="p">,</span> <span class="n">row</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">col</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">fig</span><span class="p">.</span><span class="nf">add_trace</span><span class="p">(</span><span class="n">line_trace</span><span class="p">,</span> <span class="n">row</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">col</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>

<span class="n">fig</span><span class="p">.</span><span class="nf">update_layout</span><span class="p">(</span>
    <span class="n">xaxis</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">title</span><span class="o">=</span><span class="sh">"</span><span class="s">Train time (s) - lower is better</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">yaxis</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">title</span><span class="o">=</span><span class="sh">"</span><span class="s">Test R2 score - higher is better</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">legend</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.72</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="mf">0.05</span><span class="p">,</span> <span class="n">traceorder</span><span class="o">=</span><span class="sh">"</span><span class="s">normal</span><span class="sh">"</span><span class="p">,</span> <span class="n">borderwidth</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
    <span class="n">title</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="mf">0.5</span><span class="p">,</span> <span class="n">text</span><span class="o">=</span><span class="sh">"</span><span class="s">Speed-score trade-off</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">height</span><span class="o">=</span><span class="mi">1000</span><span class="p">,</span>
    <span class="n">width</span><span class="o">=</span><span class="mi">800</span>
<span class="p">)</span>

<span class="n">fig</span><span class="p">.</span><span class="nf">write_image</span><span class="p">(</span><span class="sh">"</span><span class="s">train_time_vs_score_no_gb.svg</span><span class="sh">"</span><span class="p">)</span>
<span class="n">fig</span><span class="p">.</span><span class="nf">write_image</span><span class="p">(</span><span class="sh">"</span><span class="s">train_time_vs_score_no_gb.png</span><span class="sh">"</span><span class="p">)</span>
<span class="n">fig</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
</code></pre></div></div>]]></content><author><name>Julian Santen</name></author><category term="decision-trees"/><category term="random-forest"/><category term="boosting"/><category term="xgboost"/><summary type="html"><![CDATA[Ensemble methods combine multiple simple learning algorithms to achieve superior overall performance. This blog post is an adaptation of a group project from the CS4270 course I took at the National University of Singapore during my exchange.]]></summary></entry></feed>